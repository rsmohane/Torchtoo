import { useState, useEffect, useRef } from "react";

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  GLOBAL DATA  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
const COMPANY = {
  name:"GRT ASSIST DIGITAL CAMPAIGN", full:"GRT ASSIST DIGITAL CAMPAIGN — AUTOMATION AND SECURITY PVT LTD",
  email:"info@grt.com", phone:"+919424936248", phoneDisplay:"+91 94249 36248",
};

// ── CAMPUS LAYERS ─────────────────────────────────────────────────────────────
const CAMPUS_LAYERS = [
  { id:"learning",   label:"Learning Layer",    icon:"🎓", color:"#00C9FF",
    nodes:[
      { id:"lms",  label:"LMS Platform",  tag:"Public + Private", desc:"Adaptive course delivery — SCORM, xAPI, video, live classes for students, faculty and public auditors.", uses:["Students","Faculty","Admin","Public"] },
      { id:"vlab", label:"Virtual Lab",   tag:"Future-Ready",     desc:"24/7 simulation-based labs — Physics, Chemistry, Coding, Engineering, AI experiments.", uses:["Students","Research","Labs"] },
      { id:"exam", label:"Online Exams",  tag:"Secure + Adaptive",desc:"AI-proctored, browser-locked, auto-graded assessments with plagiarism detection.", uses:["Students","Faculty","Admin"] },
    ]},
  { id:"infra",     label:"Infrastructure Layer",icon:"🏗️",color:"#43E97B",
    nodes:[
      { id:"wifi",  label:"WiFi Network",   tag:"Resilient + Scalable",    desc:"Campus-wide mesh with load balancing, IoT zones, guest portals.",uses:["All Users","IoT","Visitors"] },
      { id:"cloud", label:"Cloud Storage",  tag:"Collaborative + Secure",  desc:"Encrypted shared drives, version control, access logs, real-time editing.",uses:["Faculty","Students","Admin"] },
      { id:"erp",   label:"Campus ERP",     tag:"Transparent + Automated", desc:"Finance, HR, admissions, inventory and timetabling in one platform.",uses:["Admin","Finance","HR"] },
    ]},
  { id:"security",  label:"Security Layer",     icon:"🔐",color:"#F7971E",
    nodes:[
      { id:"enc",label:"Data Encryption",    tag:"Trust + Compliance",  desc:"AES-256 end-to-end encryption — GDPR, FERPA, ISO 27001 compliant.",uses:["System-Wide","Compliance"] },
      { id:"idm",label:"Identity Management",tag:"Role-Based Access",   desc:"SSO, MFA, biometric login, granular permission tiers.",uses:["All Users","IT Admins"] },
      { id:"bkp",label:"Backup System",      tag:"Continuity + Resilience",desc:"Automated daily snapshots, geo-redundant DR, 99.99% SLA.",uses:["System-Wide","DR Team"] },
    ]},
  { id:"innovation",label:"Innovation Layer",   icon:"🚀",color:"#DA77FF",
    nodes:[
      { id:"ai",  label:"AI Assistant",       tag:"Personalized + Social",desc:"Claude — campus AI for learning, scheduling, research, career and mental health.",uses:["All Users","24/7"] },
      { id:"rdc", label:"Research Data Center",tag:"Big Data + Industry", desc:"Petabyte-scale repos, GPU clusters, federated learning pipelines.",uses:["Researchers","Faculty","Industry"] },
      { id:"ind", label:"Industry Integration",tag:"Jobs + Collaboration",desc:"Internship feeds, employer APIs, co-op portals, hackathon hubs.",uses:["Students","Employers","Alumni"] },
    ]},
];

// ── STUDENT STUDY ENVIRONMENTS ────────────────────────────────────────────────
const STUDY_ENV = [
  { id:"classroom", label:"Classroom Environment", icon:"🏫", color:"#00C9FF",
    types:[
      { name:"Traditional Classroom",    desc:"Blackboard-based face-to-face teaching with fixed seating and teacher-centered instruction." },
      { name:"Smart Classroom",          desc:"Interactive digital boards, audio-visual systems, cloud-connected teaching tools." },
      { name:"Digital Classroom",        desc:"Fully digitized with tablets/laptops, e-content delivery and real-time assessments." },
      { name:"Hybrid Classroom",         desc:"Blends in-person and remote learners simultaneously via live-streaming and collaboration tools." },
      { name:"Inclusive Classroom",      desc:"Adapted for differently-abled students — assistive tech, multilingual support, flexible seating." },
      { name:"Interactive Learning Env.",desc:"Activity-based, peer-discussion, gamified learning designed for engagement and retention." },
    ],
    example:"UNESCO", exampleLink:"https://www.unesco.org", exampleDesc:"Supports global educational standards and inclusive learning frameworks." },
  { id:"home", label:"Home Study Environment", icon:"🏠", color:"#43E97B",
    types:[
      { name:"Self-Study Room",          desc:"Dedicated quiet personal space with organized materials, minimal distraction." },
      { name:"Quiet Study Space",        desc:"Noise-controlled area — library corner, silent zones or soundproofed room." },
      { name:"Online Learning Setup",    desc:"Desktop/laptop with stable WiFi, headset, good lighting and ergonomic chair." },
      { name:"Virtual Classroom Env.",   desc:"Software-equipped home setup for live video classes, collaborative whiteboards and real-time chat." },
    ],
    example:"", exampleLink:"", exampleDesc:"" },
  { id:"digital", label:"Digital Study Environment", icon:"💻", color:"#DA77FF",
    types:[
      { name:"E-Learning Platform",      desc:"Browser-based courses with video lectures, quizzes and progress tracking — accessible anytime." },
      { name:"Virtual Labs",             desc:"Cloud-hosted simulation labs replacing physical experiments for remote and online learners." },
      { name:"LMS (Learning Mgmt System)",desc:"Google Classroom, Moodle, Canvas — centralized course management and student tracking." },
      { name:"AI-Based Learning Env.",   desc:"Personalized adaptive learning powered by AI — adjusts difficulty, content and pacing per student." },
    ],
    example:"Google Classroom", exampleLink:"https://classroom.google.com", exampleDesc:"Leading global LMS platform with free access for educational institutions worldwide." },
  { id:"cognitive", label:"Cognitive Learning Environment", icon:"🧠", color:"#FFD93D",
    types:[
      { name:"Focus-Based Study Zone",   desc:"Minimalist, distraction-free space engineered for deep work and concentration." },
      { name:"Collaborative Learning Space",desc:"Group tables, whiteboards, breakout areas designed for team-based problem solving." },
      { name:"Problem-Solving Env.",     desc:"Case study rooms, design thinking studios with prototyping tools and brainstorming walls." },
      { name:"Research Discussion Room", desc:"Seminar-style space with databases access, journal subscriptions and structured debate format." },
    ],
    example:"", exampleLink:"", exampleDesc:"" },
];

// ── LABORATORY ENVIRONMENTS ───────────────────────────────────────────────────
const LAB_ENV = [
  { id:"science", label:"Science Laboratory", icon:"🔬", color:"#FF6B6B",
    subs:[
      { name:"Physics Lab",    items:["Optics Setup","Mechanics Experiments","Electrical Circuits","Wave & Sound Lab"] },
      { name:"Chemistry Lab",  items:["Chemical Storage Room","Fume Hood Area","Acid-Resistant Benches","Titration Station"] },
      { name:"Biology Lab",    items:["Microscope Section","Specimen Storage","Dissection Area","Incubation Chamber"] },
    ]},
  { id:"computer", label:"Computer Laboratory", icon:"💻", color:"#00C9FF",
    subs:[
      { name:"Programming Lab",       items:["Python/Java/C++ Workstations","Code Compiler Servers","Debug Terminals","Git Integration"] },
      { name:"Networking Lab",        items:["Router/Switch Rack","Packet Tracer Systems","Network Simulation Tools","Cisco CCNA Equipment"] },
      { name:"Cybersecurity Lab",     items:["Ethical Hacking Stations","Penetration Testing Tools","SIEM Systems","Firewall Config Bench"] },
      { name:"AI & ML Lab",           items:["GPU Workstations","TensorFlow/PyTorch Environment","Data Pipeline Tools","Model Training Cluster"] },
      { name:"Cloud Computing Lab",   items:["AWS/Azure/GCP Sandbox","Container Orchestration","DevOps CI/CD Pipeline","Cloud Storage Simulation"] },
    ],
    example:"Microsoft Azure", exampleLink:"https://azure.microsoft.com" },
  { id:"engineering", label:"Engineering Laboratory", icon:"🏗", color:"#43E97B",
    subs:[
      { name:"Mechanical Workshop",   items:["Lathe Machine","CNC Machines","3D Printers","Welding Station"] },
      { name:"Civil Engineering Lab", items:["Soil Testing Equipment","Concrete Mix Lab","Structural Models","Surveying Tools"] },
      { name:"Electronics Lab",       items:["Oscilloscopes","PCB Design Tools","Soldering Stations","Signal Generators"] },
      { name:"Robotics Lab",          items:["Robot Arms","Servo Controllers","ROS Workstations","Sensor Integration Kits"] },
      { name:"IoT Innovation Lab",    items:["Raspberry Pi / Arduino Kits","MQTT Brokers","Edge Computing Nodes","Smart Sensor Arrays"] },
    ],
    example:"Siemens", exampleLink:"https://www.siemens.com" },
  { id:"medical", label:"Medical & Pharma Lab", icon:"🧬", color:"#FF77B3",
    subs:[
      { name:"Microbiology Lab",        items:["Autoclave","Culture Media Prep","Microscopy Station","Biosafety Cabinet"] },
      { name:"Pathology Lab",           items:["Tissue Processing","Histology Station","Biopsy Analysis","Slide Scanning System"] },
      { name:"Clinical Research Lab",   items:["Clinical Trial Area","Patient Data Analytics","Protocol Documentation Station","Ethics Review Room"] },
      { name:"Pharma Testing Lab",      items:["HPLC Systems","Dissolution Tester","Stability Chambers","GMP Compliance Zone"] },
    ],
    example:"World Health Organization", exampleLink:"https://www.who.int" },
  { id:"environmental", label:"Environmental Laboratory", icon:"🌱", color:"#77FFCC",
    subs:[
      { name:"Soil Testing Lab",        items:["pH Meters","Spectrophotometers","Nutrient Analysis Kits","Texture Analysis Equipment"] },
      { name:"Water Testing Lab",       items:["BOD/COD Analyzers","Turbidity Meters","Heavy Metal Detection","Microbial Testing Station"] },
      { name:"Air Quality Lab",         items:["PM2.5/PM10 Monitors","Gas Analyzers","Weather Stations","Emission Detectors"] },
      { name:"Climate Research Lab",    items:["Greenhouse Simulation","Rainfall Measurement","Carbon Footprint Calculators","GIS Mapping Tools"] },
    ]},
  { id:"advanced", label:"Advanced Research Labs", icon:"🚀", color:"#DA77FF",
    subs:[
      { name:"Space Research Lab",      items:["Telescope Systems","Orbital Simulation Software","Satellite Data Analysis","Astrophysics Computing Cluster"] },
      { name:"Nuclear Research Lab",    items:["Radiation Detection","Shielding Chambers","Nuclear Simulation Software","Safety Protocol Systems"] },
      { name:"Biotechnology Lab",       items:["Gene Sequencing","CRISPR Tools","Protein Synthesis Station","Fermentation Bioreactors"] },
      { name:"Nanotechnology Lab",      items:["Electron Microscope","Nanofabrication Tools","Clean Room Zone","Surface Analysis System"] },
    ],
    example:"NASA", exampleLink:"https://www.nasa.gov" },
];

// ── MICRO LAB CONDITIONS ──────────────────────────────────────────────────────
const MICRO_LAB = [
  { icon:"🌡️", label:"Temperature Control",   desc:"Precise HVAC maintaining 18–22°C; critical for chemical reactions, specimen integrity and equipment reliability." },
  { icon:"💧", label:"Humidity Control",       desc:"Maintained at 40–60% RH to prevent condensation on equipment, mold growth and chemical degradation." },
  { icon:"🌬️", label:"Ventilation System",    desc:"Laminar flow hoods, fume extraction, HEPA filters ensuring safe air quality and removing toxic vapors." },
  { icon:"🦺", label:"Safety Equipment",       desc:"PPE stations, safety showers, eye wash units, fire extinguishers positioned at all critical zones." },
  { icon:"🔥", label:"Fire Protection System", desc:"Automatic suppression (CO₂/FM-200), heat detectors, smoke alarms with emergency evacuation routes." },
  { icon:"🧪", label:"Chemical Storage",       desc:"Segregated cabinets — flammables, corrosives, oxidizers separately stored with secondary containment." },
  { icon:"✨", label:"Cleanroom Environment",  desc:"ISO class-controlled environments for semiconductor, biotech and nano-scale research operations." },
  { icon:"🦠", label:"Sterile Zone",          desc:"Autoclaved surfaces, UV sterilization, positive/negative pressure rooms for microbiology and clinical labs." },
];

const LAB_SAFETY = [
  { cat:"PPE",     icon:"🥽", color:"#F7971E", items:["Safety Goggles","Lab Coat","Nitrile Gloves","Face Shield","Safety Boots","Respirator Mask"] },
  { cat:"Emergency",icon:"🚨",color:"#FF6B6B", items:["Emergency Shower","Eye Wash Station","First Aid Room","Emergency Exit Signs","Assembly Point","Panic Button"] },
  { cat:"Waste",   icon:"♻️", color:"#43E97B", items:["Hazardous Waste Bins","Bio-Hazard Containers","Chemical Neutralization","Sharps Disposal","E-Waste Station","Radiation Waste (if applicable)"] },
  { cat:"Digital", icon:"🔒", color:"#00C9FF", items:["Secure Student Login","Biometric Lab Access","CCTV Monitoring","Lab Data Protection","Access Logs","Remote Lockdown System"] },
];

// ── SCHOOL ENVIRONMENT ────────────────────────────────────────────────────────
const SCHOOL_ENV = [
  { id:"academic",  label:"Academic Environment",      icon:"📚", color:"#00C9FF",
    items:["Smart Classrooms with Digital Boards","Project-Based Learning Rooms","Language Lab — Pronunciation & Comprehension","Mathematics Lab — Manipulatives & Models","Science Activity Room — Demonstration Kits","Creative Writing & Debate Studio"] },
  { id:"library",   label:"Library & Research",        icon:"📖", color:"#43E97B",
    items:["Digital Library — e-Books & Journals","Physical Book Section — Catalogued & Indexed","Research Discussion Room — Seminar Style","Online Journal Access — IEEE, Springer, Elsevier","E-Resource Center — NPTEL, Khan Academy, Coursera"],
    example:"UNESCO", exampleLink:"https://www.unesco.org" },
  { id:"tech",      label:"Technology Environment",    icon:"💻", color:"#DA77FF",
    items:["Computer Lab — OS, Office & Coding Tools","Coding Lab — Python, Scratch, Block Programming","Robotics Lab — LEGO Mindstorms, Arduino Kits","AI & ML Lab — Dataset Training, Model Testing","Cloud Lab — Google Workspace, Azure for Students"],
    example:"Microsoft Azure", exampleLink:"https://azure.microsoft.com" },
  { id:"sports",    label:"Sports & Wellness",         icon:"⚽", color:"#FFD93D",
    items:["Indoor Sports Hall — Badminton, TT, Basketball","Outdoor Ground — Cricket, Football, Athletics","Yoga & Meditation Room","Counseling & Mental Wellness Center","Nutrition & Health Awareness Corner","Rest & Recovery Room"] },
  { id:"safety_s",  label:"Safety & Infrastructure",  icon:"🛡️", color:"#FF6B6B",
    items:["Fire Alarm & Suppression System","CCTV Surveillance — All Zones","Biometric Entry Gates","First Aid Room with Trained Staff","Emergency Assembly Points","Anti-Ragging & Anti-Bullying Monitoring"] },
];

// ── UNIVERSITY / COLLEGE LABS ─────────────────────────────────────────────────
const UNI_LABS = [
  { id:"core_sci", label:"Core Science Labs", icon:"🔬", color:"#00C9FF",
    labs:["Advanced Physics Lab — Laser, Spectroscopy, Superconductivity","Organic Chemistry Lab — Synthesis, Chromatography, NMR","Biotechnology Lab — PCR, Electrophoresis, Cell Culture","Environmental Science Lab — Remote Sensing, GIS Analysis"] },
  { id:"tech_eng", label:"Technical & Engineering Labs", icon:"💻", color:"#43E97B",
    labs:["Software Development Lab — Agile Workspace, DevOps Tools","Cybersecurity Lab — SOC Simulation, Malware Analysis","Data Science Lab — Jupyter, R Studio, Big Data Cluster","Networking & CCNA Lab — Cisco Equipment, Packet Tracer","IoT Innovation Lab — Smart Sensor Networks, Edge AI"],
    example:"Cisco", exampleLink:"https://www.cisco.com" },
  { id:"medical_c", label:"Medical & Clinical Environment", icon:"🏥", color:"#FF77B3",
    labs:["Anatomy Lab — 3D Human Models, VR Body Systems","Pathology Lab — Digital Slide Scanning, AI Diagnosis","Microbiology Lab — BSL-2 Facility, Sterile Culture","Clinical Simulation Lab — Patient Mannequins, Emergency Scenarios"],
    example:"AIIMS India", exampleLink:"https://www.aiims.edu" },
  { id:"research_i",label:"Research & Innovation Center", icon:"🚀", color:"#DA77FF",
    labs:["Incubation Center — Startup Workspace, Prototyping Bay","Industry Collaboration Lab — Industry-Sponsored Projects","Patent & IP Cell — Innovation Documentation","AI Research Lab — LLM Training, Computer Vision, NLP"],
    example:"IIT Network", exampleLink:"https://www.iitb.ac.in" },
];

// ── PUBLIC MIND LAYERS ────────────────────────────────────────────────────────
const MIND_LAYERS = [
  { id:"physical",  label:"Physical Environment",   icon:"🌍", color:"#43E97B",
    definition:"Physical spaces and conditions that directly shape stress, mobility and health of the public.",
    aspects:["Urban vs Rural Areas — density, infrastructure, access","Climate & Weather — heat, rainfall, disaster risk","Noise, Pollution & Traffic Density","Public Parks, Open Spaces & Recreation Areas"],
    impact:"Stress levels, mood, creativity, productivity and physical health",
    sources:["IoT Sensors","Smart City Devices","Satellite Imagery","GPS Mobility Data"],
    metrics:["Stress Index","Health Indicators","Mobility Trends","Air Quality Index"] },
  { id:"social",    label:"Social Environment",     icon:"👥", color:"#00C9FF",
    definition:"Interpersonal and community dynamics that shape collective behavior and group identity.",
    aspects:["Family Influence — parenting, values, communication styles","Peer Groups & Community Norms","Social & Religious Ideological Influence","Cultural Expectations & Group Conformity"],
    impact:"Decision-making, behavioral patterns, group conformity and social engagement",
    sources:["Social Surveys","Mobile Apps","Community Databases","Field Research"],
    metrics:["Social Cohesion Score","Community Engagement","Trend Adoption Rate"] },
  { id:"economic",  label:"Economic & Political",   icon:"📊", color:"#F7971E",
    definition:"Financial conditions and governance frameworks shaping public confidence and civic behavior.",
    aspects:["Employment & Income Levels","Economic Stability & Inflation Impact","Political Climate & Governance Quality","Government Policies & Public Policy Awareness"],
    impact:"Risk perception, public confidence, civic participation and consumer behavior",
    sources:["Government Databases","Census Data","Economic Surveys","Political Polling"],
    metrics:["Employment Rate","Economic Stability Index","Policy Adoption","Civic Participation"] },
  { id:"educational",label:"Educational & Knowledge",icon:"📚",color:"#FF77B3",
    definition:"Access to learning and knowledge infrastructure shaping critical thinking and innovation.",
    aspects:["School & University Culture — quality, access, inclusivity","Library & Research Availability","Online Education Access — MOOCs, e-Learning Platforms","Public Awareness Campaigns & Media Literacy"],
    impact:"Critical thinking, innovation mindset and informed decision-making at population level",
    sources:["LMS Platforms","Education Boards","Research Databases","Public Survey Data"],
    metrics:["Literacy Rate","Digital Literacy Index","Innovation Adoption","Research Output"] },
  { id:"digital",   label:"Digital & Virtual",      icon:"💻", color:"#77FFCC",
    definition:"Online ecosystems and digital media shaping public opinion and information flow.",
    aspects:["Social Media Influence — Twitter, Instagram, YouTube, WhatsApp","News & Information Sources — credibility, bias, misinformation","Online Forums & Community Groups","AI-Driven Recommendation Platforms"],
    impact:"Opinion shaping, misinformation effects, trend adoption and digital polarization",
    sources:["AI NLP Engines","Social Listening Tools","Web Scraping","Cloud Analytics"],
    metrics:["Sentiment Score","Misinformation Rate","Trend Velocity","Influencer Reach"] },
  { id:"cultural",  label:"Cultural & Recreational",icon:"🎭", color:"#FFD93D",
    definition:"Shared cultural expressions, events and activities that bind communities emotionally.",
    aspects:["Arts, Cinema, Music & Literature — consumption and production","Public Events, Festivals & Religious Gatherings","Sports & Recreational Activity — local to national","Historical Heritage Sites & Cultural Identity"],
    impact:"Creativity, emotional bonding, cultural identity, collective behavior and mental wellness",
    sources:["Ticketing Systems","Cultural Databases","Media Analytics","Event APIs"],
    metrics:["Cultural Participation","Emotional Wellbeing Index","Event Attendance","Identity Score"] },
];

const PUBLIC_MIND_EXAMPLES = [
  { icon:"🗳️", label:"Voting Behavior",     desc:"Environmental and social factors determining political choices and electoral participation rates." },
  { icon:"💉", label:"Health Compliance",   desc:"Public adoption of vaccination, mask-wearing and preventive health measures driven by trust and information." },
  { icon:"📱", label:"Trend Adoption",      desc:"Fashion, technology and app adoption influenced by social media, peer groups and digital influencers." },
  { icon:"✊", label:"Social Movements",    desc:"Protest formation, civic activism and social change driven by economic pressure and political sentiment." },
  { icon:"🛒", label:"Consumer Behavior",   desc:"Purchase patterns, brand loyalty and market trends shaped by economic, digital and cultural environments." },
  { icon:"🌐", label:"Global Awareness",    desc:"Climate consciousness, pandemic response and international solidarity driven by education and digital exposure." },
];

const FUTURE_LAB = [
  { icon:"🤖", title:"AI Tutor System",          desc:"Personalized 1-on-1 AI tutor that adapts curriculum, pace and difficulty in real time." },
  { icon:"🥽", title:"VR Learning Lab",           desc:"Immersive virtual reality classrooms and historical/scientific environments for experiential learning." },
  { icon:"🔭", title:"AR Science Experiments",   desc:"Augmented reality overlays on physical objects — visualize atoms, circuits, anatomy in 3D." },
  { icon:"🏭", title:"Digital Twin Engineering", desc:"Virtual replicas of physical labs enabling safe simulation of complex engineering processes." },
  { icon:"☁️", title:"Remote Cloud Lab Access",  desc:"Full lab capabilities accessible from any device via cloud — no physical presence required." },
  { icon:"🌐", title:"Global Collaboration Plat.",desc:"Real-time cross-border student and researcher collaboration with shared project workspaces." },
];

const ETL_STEPS = [
  { id:1, icon:"📡", color:"#00C9FF", label:"IoT Sensors + Digital Platforms + Surveys + Cultural/Economic Data" },
  { id:2, icon:"⚙️", color:"#43E97B", label:"ETL Pipeline — Collect, Clean & Normalize Raw Data" },
  { id:3, icon:"🗄️", color:"#F7971E", label:"Data Warehouse / Data Lake — Centralized Aggregated Storage" },
  { id:4, icon:"🤖", color:"#DA77FF", label:"Analytics & AI Layer — ML, Sentiment, Prediction, Risk Detection" },
  { id:5, icon:"📊", color:"#FF77B3", label:"Dashboard & Visualization — Micro/Macro, Heatmaps, Alerts, Reports" },
  { id:6, icon:"🏛",  color:"#FFD93D", label:"Decision Makers / Public Access — Government, NGOs, Citizens, Investors" },
];

const AI_CAPABILITIES = [
  { icon:"💬", title:"Sentiment Analysis",    color:"#00C9FF", desc:"NLP-driven public mood detection from social media, surveys and news streams in real time." },
  { icon:"🔮", title:"Behavior Prediction",   color:"#DA77FF", desc:"Policy adoption forecasting, consumer modeling and health compliance prediction." },
  { icon:"⚠️", title:"Event Forecasting",     color:"#FF6B6B", desc:"Early warning for protests, disaster zones, festival surges via anomaly detection." },
  { icon:"📈", title:"Micro-to-Macro Analysis",color:"#43E97B",desc:"Aggregate neighborhood data upward to city, state, national and global insights." },
  { icon:"🌐", title:"Cross-Country Compare",  color:"#F7971E", desc:"Real-time global comparison of sentiment, economic health, cultural participation." },
  { icon:"🧠", title:"Graph Network Analysis", color:"#77FFCC", desc:"Social influence mapping, cluster detection and misinformation propagation tracking." },
];

const API_KEYS_GUIDE = [
  { service:"Anthropic (Claude AI)", icon:"🤖", color:"#DA77FF", keyFormat:"sk-ant-api03-...", free:"$5 credits on signup", link:"https://console.anthropic.com",
    purpose:"Powers AI chat, search, workflow summaries, prediction engine and support resolution.",
    steps:["Go to console.anthropic.com","Sign up / Login","Click API Keys → + Create Key","Copy key starting with sk-ant-...","Add to .env as REACT_APP_ANTHROPIC_KEY"] },
  { service:"Google Maps API", icon:"🗺️", color:"#43E97B", keyFormat:"AIzaSy...", free:"$200 monthly credit", link:"https://console.cloud.google.com",
    purpose:"Google Maps embed for campus geo-visualization and public mind location analytics.",
    steps:["Go to console.cloud.google.com","Create Project → Enable Maps JavaScript API","Credentials → Create API Key","Restrict key to your domain","Add to .env as REACT_APP_GOOGLE_KEY"] },
  { service:"OpenWeatherMap", icon:"🌤️", color:"#00C9FF", keyFormat:"abc123def...", free:"1000 calls/day free", link:"https://openweathermap.org/api",
    purpose:"Real-time climate & weather data for Physical Environment Layer.",
    steps:["Go to openweathermap.org/api","Sign Up free","My API Keys → copy default key","Free: 1,000 API calls/day","Add as REACT_APP_WEATHER_KEY"] },
  { service:"NewsAPI", icon:"📰", color:"#FF77B3", keyFormat:"abc123...", free:"100 req/day free", link:"https://newsapi.org",
    purpose:"Live news for public opinion, cultural events, economic and political data feed.",
    steps:["Go to newsapi.org","Get API Key — free signup","Verify email","Copy key from dashboard","Add as REACT_APP_NEWS_KEY"] },
  { service:"Twitter / X API", icon:"🐦", color:"#1DA1F2", keyFormat:"AAAA... Bearer Token", free:"Basic tier free", link:"https://developer.twitter.com",
    purpose:"Social media monitoring, sentiment tracking, trend detection for Digital Layer.",
    steps:["Go to developer.twitter.com","Apply for Developer Account","Create App → Generate Bearer Token","Free tier: read access","Use in Social Media monitoring tab"] },
];

const GITHUB_STEPS = [
  { step:1, icon:"📁", title:"Create GitHub Repo",      desc:"github.com → New Repo → Name: grt-assist-digital-campaign → Public or Private → Create" },
  { step:2, icon:"💻", title:"Install Node.js + Git",   desc:"nodejs.org (v18+) + git-scm.com → verify: node --version && git --version" },
  { step:3, icon:"⚛️", title:"Create React PWA App",    desc:"npx create-react-app grt-campus --template cra-template-pwa\ncd grt-campus\nnpm install" },
  { step:4, icon:"📋", title:"Add Platform Code",       desc:"Paste downloaded .jsx content into src/App.jsx\nnpm install recharts lucide-react" },
  { step:5, icon:"🔑", title:"Setup .env File",         desc:"REACT_APP_ANTHROPIC_KEY=sk-ant-...\nREACT_APP_GOOGLE_KEY=AIzaSy...\nREACT_APP_NEWS_KEY=abc..." },
  { step:6, icon:"📤", title:"Push to GitHub",          desc:"git init && git add .\ngit commit -m 'GRT Platform v4'\ngit remote add origin https://github.com/YOUR_USER/grt-assist-digital-campaign.git\ngit push -u origin main" },
  { step:7, icon:"🌐", title:"Deploy to Vercel",        desc:"vercel.com → Import GitHub repo → Add .env variables → Deploy → Live URL ready" },
  { step:8, icon:"📱", title:"Generate APK",            desc:"1. Deploy to Vercel first\n2. Go to pwabuilder.com → Enter your URL\n3. Build → Android → Download .apk package\n4. Install on Android via Developer Mode" },
];

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  HELPERS  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function rgb(hex){ const r=parseInt(hex.slice(1,3),16),g=parseInt(hex.slice(3,5),16),b=parseInt(hex.slice(5,7),16); return `${r},${g},${b}`; }

async function claude(messages, webSearch=false, sys="") {
  const body={ model:"claude-sonnet-4-20250514", max_tokens:1000,
    system:sys||"You are Claude AI for GRT Assist Digital Campaign — Digital Campus & Public Mind Platform. Be concise and helpful.", messages };
  if(webSearch) body.tools=[{type:"web_search_20250305",name:"web_search"}];
  const res=await fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});
  const d=await res.json();
  return d.content?.map(b=>b.type==="text"?b.text:"").filter(Boolean).join("\n")||"No response.";
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  UI ATOMS  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
const Card=({children,color="#00C9FF",style={}})=>(
  <div style={{background:`rgba(${rgb(color)},0.05)`,border:`1px solid ${color}25`,borderRadius:"14px",padding:"16px 18px",...style}}>{children}</div>
);
const Tag=({label,color})=>(
  <span style={{fontSize:"9px",background:`rgba(${rgb(color)},0.15)`,color,border:`1px solid ${color}40`,borderRadius:"4px",padding:"2px 8px",letterSpacing:"0.5px",display:"inline-block"}}>{label}</span>
);
const SH=({label})=>(
  <div style={{fontSize:"10px",color:"rgba(200,220,255,0.35)",letterSpacing:"3px",marginBottom:"14px",textAlign:"center"}}>── {label} ──</div>
);
const Btn=({onClick,children,color="#00C9FF",disabled,style={}})=>(
  <button onClick={onClick} disabled={disabled} style={{background:`rgba(${rgb(color)},0.18)`,border:`1px solid ${color}60`,color,borderRadius:"8px",padding:"10px 18px",cursor:"pointer",fontSize:"11px",letterSpacing:"1px",fontFamily:"inherit",...style}}>{children}</button>
);
const Input=({value,onChange,onKeyDown,placeholder,style={}})=>(
  <input value={value} onChange={onChange} onKeyDown={onKeyDown} placeholder={placeholder}
    style={{flex:1,background:"rgba(255,255,255,0.05)",border:"1px solid rgba(0,150,255,0.3)",borderRadius:"10px",padding:"11px 14px",color:"#E0F0FF",fontSize:"12px",outline:"none",fontFamily:"inherit",...style}}/>
);

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: CAMPUS LAYERS  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function CampusTab(){
  const [active,setActive]=useState(null);
  const [node,setNode]=useState(null);
  const lay=CAMPUS_LAYERS.find(l=>l.id===active);
  const nd=lay?.nodes.find(n=>n.id===node);
  return(
    <div>
      <SH label="DIGITAL CAMPUS ENVIRONMENT"/>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(255px,1fr))",gap:"12px",marginBottom:"14px"}}>
        {CAMPUS_LAYERS.map(l=>(
          <div key={l.id} onClick={()=>{setActive(l.id);setNode(null);}} style={{
            background:active===l.id?`rgba(${rgb(l.color)},0.1)`:"rgba(255,255,255,0.03)",
            border:`1px solid ${active===l.id?l.color:"rgba(255,255,255,0.08)"}`,
            borderRadius:"14px",padding:"16px",cursor:"pointer",transition:"all 0.25s",
            boxShadow:active===l.id?`0 0 22px rgba(${rgb(l.color)},0.12)`:"none"}}>
            <div style={{display:"flex",alignItems:"center",gap:"10px",marginBottom:"12px"}}>
              <span style={{fontSize:"20px"}}>{l.icon}</span>
              <span style={{fontSize:"12px",fontWeight:"800",color:l.color,letterSpacing:"1px"}}>{l.label.toUpperCase()}</span>
            </div>
            {l.nodes.map(n=>(
              <div key={n.id} onClick={e=>{e.stopPropagation();setActive(l.id);setNode(n.id);}} style={{
                background:node===n.id?`rgba(${rgb(l.color)},0.15)`:"rgba(255,255,255,0.04)",
                border:`1px solid ${node===n.id?l.color+"80":"rgba(255,255,255,0.06)"}`,
                borderRadius:"8px",padding:"9px 12px",cursor:"pointer",transition:"all 0.2s",marginBottom:"6px"}}>
                <div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}>
                  <span style={{fontSize:"12px",fontWeight:"700"}}>{n.label}</span>
                  <Tag label={n.tag} color={l.color}/>
                </div>
              </div>
            ))}
          </div>
        ))}
      </div>
      {nd&&lay&&(
        <Card color={lay.color}>
          <div style={{display:"flex",gap:"14px",alignItems:"flex-start"}}>
            <span style={{fontSize:"26px"}}>{lay.icon}</span>
            <div style={{flex:1}}>
              <div style={{fontSize:"15px",fontWeight:"800",color:lay.color,marginBottom:"6px"}}>{nd.label}</div>
              <p style={{fontSize:"12px",color:"rgba(200,220,255,0.75)",lineHeight:"1.75",margin:"0 0 10px"}}>{nd.desc}</p>
              <div style={{display:"flex",flexWrap:"wrap",gap:"6px"}}>
                {nd.uses.map(u=><Tag key={u} label={`USER: ${u}`} color={lay.color}/>)}
              </div>
            </div>
          </div>
        </Card>
      )}
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: STUDENT STUDY ENVIRONMENT  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function StudyTab(){
  const [active,setActive]=useState(null);
  const env=STUDY_ENV.find(e=>e.id===active);
  return(
    <div>
      <SH label="STUDENT STUDY ENVIRONMENT — ALL TYPES"/>
      <div style={{background:"rgba(0,150,255,0.05)",border:"1px solid rgba(0,150,255,0.18)",borderRadius:"12px",padding:"12px 16px",marginBottom:"16px",fontSize:"11.5px",color:"rgba(200,220,255,0.6)",lineHeight:"1.7"}}>
        Complete study environment model covering Classroom, Home, Digital and Cognitive learning spaces — aligned with UNESCO, Google Classroom and global EdTech standards.
      </div>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(240px,1fr))",gap:"12px",marginBottom:"16px"}}>
        {STUDY_ENV.map(e=>(
          <div key={e.id} onClick={()=>setActive(active===e.id?null:e.id)} style={{
            background:active===e.id?`rgba(${rgb(e.color)},0.1)`:"rgba(255,255,255,0.03)",
            border:`1px solid ${active===e.id?e.color:"rgba(255,255,255,0.08)"}`,
            borderRadius:"14px",padding:"16px",cursor:"pointer",transition:"all 0.25s"}}>
            <div style={{display:"flex",alignItems:"center",gap:"10px",marginBottom:"10px"}}>
              <span style={{fontSize:"22px"}}>{e.icon}</span>
              <span style={{fontSize:"12px",fontWeight:"800",color:e.color}}>{e.label}</span>
            </div>
            <div style={{fontSize:"11px",color:"rgba(200,220,255,0.45)",marginBottom:"8px"}}>{e.types.length} environment types</div>
            <div style={{fontSize:"10px",color:e.color}}>{active===e.id?"▲ COLLAPSE":"▼ VIEW TYPES"}</div>
          </div>
        ))}
      </div>
      {env&&(
        <Card color={env.color} style={{marginBottom:"14px"}}>
          <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:"14px",flexWrap:"wrap",gap:"8px"}}>
            <div style={{display:"flex",gap:"10px",alignItems:"center"}}>
              <span style={{fontSize:"24px"}}>{env.icon}</span>
              <span style={{fontSize:"15px",fontWeight:"800",color:env.color}}>{env.label}</span>
            </div>
            {env.example&&(
              <a href={env.exampleLink} target="_blank" rel="noreferrer" style={{display:"flex",alignItems:"center",gap:"6px",background:`rgba(${rgb(env.color)},0.12)`,border:`1px solid ${env.color}40`,color:env.color,borderRadius:"8px",padding:"6px 12px",textDecoration:"none",fontSize:"10px",letterSpacing:"1px"}}>
                🔗 {env.example} ↗
              </a>
            )}
          </div>
          {env.example&&<div style={{fontSize:"11px",color:"rgba(200,220,255,0.5)",marginBottom:"14px",padding:"8px 12px",background:"rgba(255,255,255,0.03)",borderRadius:"8px"}}>{env.exampleDesc}</div>}
          <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))",gap:"10px"}}>
            {env.types.map((t,i)=>(
              <div key={i} style={{background:"rgba(255,255,255,0.04)",border:"1px solid rgba(255,255,255,0.07)",borderRadius:"10px",padding:"12px 14px"}}>
                <div style={{fontSize:"12px",fontWeight:"800",color:env.color,marginBottom:"6px"}}>
                  <span style={{fontSize:"10px",color:"rgba(200,220,255,0.4)",fontWeight:"normal",marginRight:"6px"}}>TYPE {i+1}</span>{t.name}
                </div>
                <p style={{fontSize:"11px",color:"rgba(200,220,255,0.65)",lineHeight:"1.65",margin:0}}>{t.desc}</p>
              </div>
            ))}
          </div>
        </Card>
      )}
      {/* Future lab section */}
      <div>
        <div style={{fontSize:"11px",color:"rgba(200,220,255,0.4)",letterSpacing:"2px",marginBottom:"12px"}}>🚀 FUTURE STUDENT LAB ENVIRONMENTS</div>
        <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(240px,1fr))",gap:"10px"}}>
          {FUTURE_LAB.map((f,i)=>(
            <div key={i} style={{background:"rgba(218,119,255,0.05)",border:"1px solid rgba(218,119,255,0.2)",borderRadius:"12px",padding:"14px 16px"}}>
              <div style={{display:"flex",alignItems:"center",gap:"8px",marginBottom:"8px"}}>
                <span style={{fontSize:"20px"}}>{f.icon}</span>
                <span style={{fontSize:"11.5px",fontWeight:"800",color:"#DA77FF"}}>{f.title}</span>
              </div>
              <p style={{fontSize:"11px",color:"rgba(200,220,255,0.6)",lineHeight:"1.65",margin:0}}>{f.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: LABORATORY ENVIRONMENT  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function LabTab(){
  const [active,setActive]=useState(null);
  const [sub,setSub]=useState(null);
  const lab=LAB_ENV.find(l=>l.id===active);
  const subData=lab?.subs.find(s=>s.name===sub);
  return(
    <div>
      <SH label="LABORATORY ENVIRONMENT — ALL TYPES"/>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(200px,1fr))",gap:"10px",marginBottom:"16px"}}>
        {LAB_ENV.map(l=>(
          <div key={l.id} onClick={()=>{setActive(active===l.id?null:l.id);setSub(null);}} style={{
            background:active===l.id?`rgba(${rgb(l.color)},0.1)`:"rgba(255,255,255,0.03)",
            border:`1px solid ${active===l.id?l.color:"rgba(255,255,255,0.08)"}`,
            borderRadius:"12px",padding:"14px",cursor:"pointer",transition:"all 0.25s"}}>
            <div style={{display:"flex",alignItems:"center",gap:"8px",marginBottom:"8px"}}>
              <span style={{fontSize:"22px"}}>{l.icon}</span>
              <span style={{fontSize:"11.5px",fontWeight:"800",color:l.color}}>{l.label}</span>
            </div>
            <div style={{fontSize:"10px",color:"rgba(200,220,255,0.4)"}}>{l.subs.length} sub-labs</div>
            {l.example&&<div style={{marginTop:"8px",fontSize:"9px",color:l.color}}>eg: {l.example}</div>}
          </div>
        ))}
      </div>
      {lab&&(
        <Card color={lab.color} style={{marginBottom:"16px"}}>
          <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:"14px",flexWrap:"wrap",gap:"8px"}}>
            <div style={{display:"flex",gap:"10px",alignItems:"center"}}>
              <span style={{fontSize:"24px"}}>{lab.icon}</span>
              <span style={{fontSize:"15px",fontWeight:"800",color:lab.color}}>{lab.label}</span>
            </div>
            {lab.example&&(
              <a href={lab.exampleLink} target="_blank" rel="noreferrer" style={{fontSize:"10px",background:`rgba(${rgb(lab.color)},0.12)`,border:`1px solid ${lab.color}40`,color:lab.color,borderRadius:"8px",padding:"6px 12px",textDecoration:"none",letterSpacing:"1px"}}>
                🔗 {lab.example} ↗
              </a>
            )}
          </div>
          <div style={{display:"flex",flexWrap:"wrap",gap:"8px",marginBottom:"14px"}}>
            {lab.subs.map(s=>(
              <div key={s.name} onClick={()=>setSub(sub===s.name?null:s.name)} style={{
                background:sub===s.name?`rgba(${rgb(lab.color)},0.18)`:"rgba(255,255,255,0.05)",
                border:`1px solid ${sub===s.name?lab.color:"rgba(255,255,255,0.1)"}`,
                borderRadius:"8px",padding:"8px 14px",cursor:"pointer",fontSize:"11.5px",
                fontWeight:"700",color:sub===s.name?lab.color:"rgba(200,220,255,0.65)",transition:"all 0.2s"}}>
                {s.name}
              </div>
            ))}
          </div>
          {subData&&(
            <div style={{background:"rgba(255,255,255,0.04)",border:"1px solid rgba(255,255,255,0.08)",borderRadius:"10px",padding:"14px"}}>
              <div style={{fontSize:"11px",color:lab.color,letterSpacing:"2px",marginBottom:"10px"}}>{subData.name.toUpperCase()} — EQUIPMENT & ZONES</div>
              <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(180px,1fr))",gap:"8px"}}>
                {subData.items.map((item,i)=>(
                  <div key={i} style={{display:"flex",gap:"8px",alignItems:"center"}}>
                    <div style={{width:"5px",height:"5px",borderRadius:"50%",background:lab.color,flexShrink:0}}/>
                    <span style={{fontSize:"11.5px",color:"rgba(200,220,255,0.75)"}}>{item}</span>
                  </div>
                ))}
              </div>
            </div>
          )}
        </Card>
      )}
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: MICRO LAB + SAFETY  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function MicroLabTab(){
  return(
    <div>
      <SH label="MICRO LAB CONDITIONS + LAB SAFETY ENVIRONMENT"/>
      <div style={{marginBottom:"20px"}}>
        <div style={{fontSize:"11px",color:"rgba(200,220,255,0.4)",letterSpacing:"2px",marginBottom:"12px"}}>🏢 MICRO LAB ENVIRONMENT — CRITICAL SETUP CONDITIONS</div>
        <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))",gap:"10px"}}>
          {MICRO_LAB.map((m,i)=>(
            <div key={i} style={{background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.08)",borderRadius:"12px",padding:"14px 16px",display:"flex",gap:"12px",alignItems:"flex-start"}}>
              <span style={{fontSize:"26px",flexShrink:0}}>{m.icon}</span>
              <div>
                <div style={{fontSize:"12px",fontWeight:"800",color:"#00C9FF",marginBottom:"6px"}}>{m.label}</div>
                <p style={{fontSize:"11px",color:"rgba(200,220,255,0.65)",lineHeight:"1.65",margin:0}}>{m.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
      <div>
        <div style={{fontSize:"11px",color:"rgba(200,220,255,0.4)",letterSpacing:"2px",marginBottom:"12px"}}>🛡 LAB SAFETY & COMPLIANCE ENVIRONMENT</div>
        <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(240px,1fr))",gap:"12px"}}>
          {LAB_SAFETY.map((s,i)=>(
            <div key={i} style={{background:`rgba(${rgb(s.color)},0.06)`,border:`1px solid ${s.color}30`,borderRadius:"14px",padding:"16px"}}>
              <div style={{display:"flex",alignItems:"center",gap:"10px",marginBottom:"12px"}}>
                <span style={{fontSize:"22px"}}>{s.icon}</span>
                <span style={{fontSize:"13px",fontWeight:"800",color:s.color,letterSpacing:"1px"}}>{s.cat}</span>
              </div>
              <div style={{display:"flex",flexWrap:"wrap",gap:"6px"}}>
                {s.items.map((item,j)=>(
                  <div key={j} style={{background:"rgba(255,255,255,0.04)",border:`1px solid ${s.color}20`,borderRadius:"6px",padding:"5px 10px",fontSize:"11px",color:"rgba(200,220,255,0.72)"}}>
                    {item}
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      </div>
      <div style={{marginTop:"16px"}}>
        <div style={{fontSize:"11px",color:"rgba(200,220,255,0.4)",letterSpacing:"2px",marginBottom:"12px"}}>🌍 MICRO STUDENT ENVIRONMENT — PERFORMANCE FACTORS</div>
        <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(200px,1fr))",gap:"10px"}}>
          {[
            {icon:"💡",f:"Lighting Level",d:"Natural + artificial lighting 300-500 lux, glare-free for screen and book reading"},
            {icon:"🔇",f:"Noise Control",d:"Acoustic panels, double-glazed windows, <40dB ambient noise level"},
            {icon:"🌡️",f:"Temperature Control",d:"18–22°C thermostat-controlled HVAC with zonal management"},
            {icon:"💺",f:"Ergonomic Seating",d:"Height-adjustable chairs, lumbar support, desk at elbow height"},
            {icon:"🧘",f:"Mental Wellness",d:"Counseling access, mindfulness spaces, stress-free zone designation"},
            {icon:"💨",f:"Air Quality",d:"CO₂ <1000ppm, HEPA-filtered ventilation, plant installations"},
          ].map((item,i)=>(
            <div key={i} style={{background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.07)",borderRadius:"10px",padding:"12px 14px",display:"flex",gap:"10px",alignItems:"flex-start"}}>
              <span style={{fontSize:"22px",flexShrink:0}}>{item.icon}</span>
              <div>
                <div style={{fontSize:"12px",fontWeight:"700",color:"#FFD93D",marginBottom:"4px"}}>{item.f}</div>
                <p style={{fontSize:"10.5px",color:"rgba(200,220,255,0.6)",lineHeight:"1.55",margin:0}}>{item.d}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: SCHOOL ENVIRONMENT  ░══════════════════════════════════════════════
// ═══════════════════════════════════════════════════════════════════════════════
function SchoolTab(){
  const [active,setActive]=useState(null);
  const env=SCHOOL_ENV.find(e=>e.id===active);
  return(
    <div>
      <SH label="COMPLETE SCHOOL ENVIRONMENT STRUCTURE"/>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(200px,1fr))",gap:"10px",marginBottom:"16px"}}>
        {SCHOOL_ENV.map(e=>(
          <div key={e.id} onClick={()=>setActive(active===e.id?null:e.id)} style={{
            background:active===e.id?`rgba(${rgb(e.color)},0.1)`:"rgba(255,255,255,0.03)",
            border:`1px solid ${active===e.id?e.color:"rgba(255,255,255,0.08)"}`,
            borderRadius:"12px",padding:"14px",cursor:"pointer",transition:"all 0.25s"}}>
            <div style={{display:"flex",alignItems:"center",gap:"8px",marginBottom:"8px"}}>
              <span style={{fontSize:"22px"}}>{e.icon}</span>
              <span style={{fontSize:"11.5px",fontWeight:"800",color:e.color}}>{e.label}</span>
            </div>
            <div style={{fontSize:"10px",color:"rgba(200,220,255,0.4)"}}>{e.items.length} components</div>
          </div>
        ))}
      </div>
      {env&&(
        <Card color={env.color} style={{marginBottom:"14px"}}>
          <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:"14px",flexWrap:"wrap",gap:"8px"}}>
            <div style={{display:"flex",gap:"10px",alignItems:"center"}}>
              <span style={{fontSize:"24px"}}>{env.icon}</span>
              <span style={{fontSize:"15px",fontWeight:"800",color:env.color}}>{env.label}</span>
            </div>
            {env.example&&(
              <a href={env.exampleLink} target="_blank" rel="noreferrer" style={{fontSize:"10px",background:`rgba(${rgb(env.color)},0.12)`,border:`1px solid ${env.color}40`,color:env.color,borderRadius:"8px",padding:"6px 12px",textDecoration:"none",letterSpacing:"1px"}}>🔗 {env.example} ↗</a>
            )}
          </div>
          <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(200px,1fr))",gap:"8px"}}>
            {env.items.map((item,i)=>(
              <div key={i} style={{display:"flex",gap:"8px",alignItems:"flex-start",background:"rgba(255,255,255,0.04)",borderRadius:"8px",padding:"10px 12px"}}>
                <div style={{width:"6px",height:"6px",borderRadius:"50%",background:env.color,flexShrink:0,marginTop:"4px"}}/>
                <span style={{fontSize:"11.5px",color:"rgba(200,220,255,0.75)",lineHeight:"1.5"}}>{item}</span>
              </div>
            ))}
          </div>
        </Card>
      )}
      {/* Structure tree */}
      <Card color="#00C9FF">
        <div style={{fontSize:"10px",color:"#00C9FF",letterSpacing:"2px",marginBottom:"12px"}}>STUDENT ENVIRONMENT STRUCTURE TREE</div>
        <pre style={{margin:0,fontSize:"11px",color:"rgba(200,220,255,0.7)",lineHeight:"1.8",fontFamily:"'Courier New',monospace"}}>
{`STUDENT ENVIRONMENT
│
├── Study Environment
│     ├── Classroom
│     ├── Digital Learning
│     ├── Library
│     └── Research Hall
│
├── Laboratory Environment
│     ├── Science Labs
│     ├── Computer Labs
│     ├── Engineering Labs
│     └── Medical Labs
│
├── Safety Environment
│     ├── Fire Safety
│     ├── Health & Hygiene
│     └── Emergency System
│
└── Technical Infrastructure
      ├── WiFi Network
      ├── Cloud Access
      ├── LMS Platform
      └── Data Security`}
        </pre>
      </Card>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: UNIVERSITY / ADVANCED LABS  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function UniLabTab(){
  const [active,setActive]=useState(null);
  const lab=UNI_LABS.find(l=>l.id===active);
  return(
    <div>
      <SH label="UNIVERSITY / COLLEGE — ADVANCED LAB ENVIRONMENT"/>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(240px,1fr))",gap:"12px",marginBottom:"16px"}}>
        {UNI_LABS.map(l=>(
          <div key={l.id} onClick={()=>setActive(active===l.id?null:l.id)} style={{
            background:active===l.id?`rgba(${rgb(l.color)},0.1)`:"rgba(255,255,255,0.03)",
            border:`1px solid ${active===l.id?l.color:"rgba(255,255,255,0.08)"}`,
            borderRadius:"14px",padding:"16px",cursor:"pointer",transition:"all 0.25s"}}>
            <div style={{display:"flex",alignItems:"center",gap:"10px",marginBottom:"10px"}}>
              <span style={{fontSize:"22px"}}>{l.icon}</span>
              <span style={{fontSize:"12px",fontWeight:"800",color:l.color}}>{l.label}</span>
            </div>
            <div style={{fontSize:"11px",color:"rgba(200,220,255,0.45)",marginBottom:"8px"}}>{l.labs.length} labs included</div>
            {l.example&&<div style={{fontSize:"10px",color:l.color}}>eg: {l.example}</div>}
          </div>
        ))}
      </div>
      {lab&&(
        <Card color={lab.color} style={{marginBottom:"14px"}}>
          <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:"14px",flexWrap:"wrap",gap:"8px"}}>
            <div style={{display:"flex",gap:"10px",alignItems:"center"}}>
              <span style={{fontSize:"24px"}}>{lab.icon}</span>
              <span style={{fontSize:"15px",fontWeight:"800",color:lab.color}}>{lab.label}</span>
            </div>
            {lab.example&&(
              <a href={lab.exampleLink} target="_blank" rel="noreferrer" style={{fontSize:"10px",background:`rgba(${rgb(lab.color)},0.12)`,border:`1px solid ${lab.color}40`,color:lab.color,borderRadius:"8px",padding:"6px 12px",textDecoration:"none",letterSpacing:"1px"}}>🔗 {lab.example} ↗</a>
            )}
          </div>
          <div style={{display:"flex",flexDirection:"column",gap:"8px"}}>
            {lab.labs.map((l,i)=>(
              <div key={i} style={{display:"flex",gap:"12px",alignItems:"flex-start",background:"rgba(255,255,255,0.04)",borderRadius:"10px",padding:"12px 14px"}}>
                <div style={{width:"24px",height:"24px",borderRadius:"50%",background:`rgba(${rgb(lab.color)},0.2)`,color:lab.color,fontSize:"10px",fontWeight:"800",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0}}>{i+1}</div>
                <span style={{fontSize:"12px",color:"rgba(200,220,255,0.8)",lineHeight:"1.5"}}>{l}</span>
              </div>
            ))}
          </div>
        </Card>
      )}
      {/* Research & Innovation */}
      <div>
        <div style={{fontSize:"11px",color:"rgba(200,220,255,0.4)",letterSpacing:"2px",marginBottom:"12px"}}>🧪 RESEARCH & INNOVATION ENVIRONMENT</div>
        <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(240px,1fr))",gap:"10px"}}>
          {[
            {icon:"💡",title:"Incubation Center",items:["Startup Workspace","Prototype Lab","Investor Meeting Room","IP & Patent Cell","MVP Testing Zone"]},
            {icon:"🏭",title:"Industry Collaboration Lab",items:["Industry-Sponsored Lab","Internship Research Unit","Live Project Zone","Employer Partnership Desk","R&D Joint Projects"]},
            {icon:"🎓",title:"Innovation Hub",items:["Hackathon Arena","Design Thinking Studio","Open Innovation Lab","Tech Transfer Office","Student-Faculty Joint Projects"]},
          ].map((s,i)=>(
            <div key={i} style={{background:"rgba(255,215,0,0.05)",border:"1px solid rgba(255,215,0,0.2)",borderRadius:"12px",padding:"14px 16px"}}>
              <div style={{display:"flex",gap:"8px",alignItems:"center",marginBottom:"10px"}}>
                <span style={{fontSize:"22px"}}>{s.icon}</span>
                <span style={{fontSize:"12px",fontWeight:"800",color:"#FFD93D"}}>{s.title}</span>
              </div>
              {s.items.map((item,j)=>(
                <div key={j} style={{display:"flex",gap:"8px",padding:"4px 0",borderBottom:"1px solid rgba(255,255,255,0.04)"}}>
                  <span style={{color:"#FFD93D",fontSize:"10px"}}>▸</span>
                  <span style={{fontSize:"11px",color:"rgba(200,220,255,0.7)"}}>{item}</span>
                </div>
              ))}
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: PUBLIC MIND  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function MindTab(){
  const [active,setActive]=useState(null);
  const lay=MIND_LAYERS.find(l=>l.id===active);
  return(
    <div>
      <SH label="GLOBAL PUBLIC MIND ACTIVITY ENVIRONMENT"/>
      <div style={{background:"rgba(0,150,255,0.05)",border:"1px solid rgba(0,150,255,0.2)",borderRadius:"12px",padding:"14px 16px",marginBottom:"16px",fontSize:"11.5px",color:"rgba(200,220,255,0.65)",lineHeight:"1.75"}}>
        <strong style={{color:"#00C9FF"}}>Public Mind Activity</strong> refers to the collective thoughts, attitudes and behavioral patterns of a population — influenced by Physical, Social, Economic, Educational, Digital and Cultural environments from micro (neighborhood/school) to macro (global) level.
      </div>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(240px,1fr))",gap:"12px",marginBottom:"16px"}}>
        {MIND_LAYERS.map(l=>(
          <div key={l.id} onClick={()=>setActive(active===l.id?null:l.id)} style={{
            background:active===l.id?`rgba(${rgb(l.color)},0.1)`:"rgba(255,255,255,0.03)",
            border:`1px solid ${active===l.id?l.color:"rgba(255,255,255,0.08)"}`,
            borderRadius:"14px",padding:"16px",cursor:"pointer",transition:"all 0.25s"}}>
            <div style={{display:"flex",alignItems:"center",gap:"10px",marginBottom:"8px"}}>
              <span style={{fontSize:"22px"}}>{l.icon}</span>
              <span style={{fontSize:"12px",fontWeight:"800",color:l.color}}>{l.label}</span>
            </div>
            <p style={{fontSize:"11px",color:"rgba(200,220,255,0.5)",lineHeight:"1.55",margin:"0 0 8px"}}>{l.definition.slice(0,70)}…</p>
            <div style={{fontSize:"10px",color:l.color}}>{active===l.id?"▲ COLLAPSE":"▼ FULL DETAILS"}</div>
          </div>
        ))}
      </div>
      {lay&&(
        <Card color={lay.color} style={{marginBottom:"16px"}}>
          <div style={{display:"flex",gap:"10px",alignItems:"center",marginBottom:"12px"}}>
            <span style={{fontSize:"24px"}}>{lay.icon}</span>
            <div>
              <div style={{fontSize:"15px",fontWeight:"800",color:lay.color}}>{lay.label}</div>
              <div style={{fontSize:"11px",color:"rgba(200,220,255,0.55)",marginTop:"3px"}}>{lay.definition}</div>
            </div>
          </div>
          <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(200px,1fr))",gap:"14px"}}>
            <div>
              <div style={{fontSize:"10px",color:lay.color,letterSpacing:"2px",marginBottom:"8px"}}>KEY ASPECTS</div>
              {lay.aspects.map((a,i)=>(
                <div key={i} style={{display:"flex",gap:"8px",padding:"6px 0",borderBottom:"1px solid rgba(255,255,255,0.05)",alignItems:"flex-start"}}>
                  <span style={{color:lay.color,flexShrink:0}}>▸</span>
                  <span style={{fontSize:"11.5px",color:"rgba(200,220,255,0.7)",lineHeight:"1.5"}}>{a}</span>
                </div>
              ))}
            </div>
            <div>
              <div style={{fontSize:"10px",color:lay.color,letterSpacing:"2px",marginBottom:"8px"}}>IMPACT</div>
              <div style={{fontSize:"11.5px",color:"rgba(200,220,255,0.7)",lineHeight:"1.65",marginBottom:"14px",padding:"8px 12px",background:"rgba(255,255,255,0.04)",borderRadius:"8px"}}>{lay.impact}</div>
              <div style={{fontSize:"10px",color:lay.color,letterSpacing:"2px",marginBottom:"8px"}}>METRICS</div>
              <div style={{display:"flex",flexWrap:"wrap",gap:"6px",marginBottom:"12px"}}>
                {lay.metrics.map(m=><Tag key={m} label={m} color={lay.color}/>)}
              </div>
              <div style={{fontSize:"10px",color:lay.color,letterSpacing:"2px",marginBottom:"6px"}}>DATA SOURCES</div>
              <div style={{display:"flex",flexWrap:"wrap",gap:"6px"}}>
                {lay.sources.map(s=><Tag key={s} label={s} color={lay.color}/>)}
              </div>
            </div>
          </div>
        </Card>
      )}
      {/* Public Mind Examples */}
      <div style={{marginBottom:"14px"}}>
        <div style={{fontSize:"11px",color:"rgba(200,220,255,0.4)",letterSpacing:"2px",marginBottom:"12px"}}>🌱 PUBLIC MIND ACTIVITY EXAMPLES</div>
        <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:"10px"}}>
          {PUBLIC_MIND_EXAMPLES.map((e,i)=>(
            <div key={i} style={{background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.08)",borderRadius:"10px",padding:"12px 14px",display:"flex",gap:"10px",alignItems:"flex-start"}}>
              <span style={{fontSize:"22px",flexShrink:0}}>{e.icon}</span>
              <div>
                <div style={{fontSize:"12px",fontWeight:"700",color:"#43E97B",marginBottom:"4px"}}>{e.label}</div>
                <p style={{fontSize:"11px",color:"rgba(200,220,255,0.6)",lineHeight:"1.55",margin:0}}>{e.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
      {/* ETL Flow */}
      <div>
        <div style={{fontSize:"11px",color:"rgba(200,220,255,0.4)",letterSpacing:"2px",marginBottom:"12px"}}>💾 DATA FLOW ARCHITECTURE — ETL PIPELINE</div>
        <div style={{display:"flex",flexDirection:"column",gap:"0",alignItems:"center"}}>
          {ETL_STEPS.map((s,i)=>(
            <div key={s.id} style={{width:"100%",maxWidth:"640px",display:"flex",flexDirection:"column",alignItems:"center"}}>
              <div style={{width:"100%",background:`rgba(${rgb(s.color)},0.07)`,border:`1px solid ${s.color}40`,borderRadius:"10px",padding:"11px 16px",display:"flex",alignItems:"center",gap:"12px"}}>
                <span style={{fontSize:"22px"}}>{s.icon}</span>
                <div>
                  <div style={{fontSize:"10px",fontWeight:"700",color:s.color,marginBottom:"2px"}}>STEP {s.id}</div>
                  <div style={{fontSize:"11px",color:"rgba(200,220,255,0.7)",lineHeight:"1.5"}}>{s.label}</div>
                </div>
              </div>
              {i<ETL_STEPS.length-1&&<div style={{fontSize:"18px",color:"rgba(200,220,255,0.25)",padding:"2px 0",lineHeight:1}}>▼</div>}
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: AI PREDICTION  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function AIPredTab(){
  const [query,setQuery]=useState("");
  const [result,setResult]=useState("");
  const [loading,setLoading]=useState(false);
  const [mode,setMode]=useState("sentiment");
  const modes=[
    {id:"sentiment",label:"Sentiment Analysis",icon:"💬",prompt:"Analyze current public sentiment about: "},
    {id:"behavior",label:"Behavior Prediction",icon:"🔮",prompt:"Predict public behavior and trends for: "},
    {id:"event",label:"Event Forecasting",icon:"⚠️",prompt:"Forecast potential events, risks or emergencies related to: "},
    {id:"policy",label:"Policy Impact",icon:"📋",prompt:"Analyze public policy impact and likelihood for: "},
    {id:"market",label:"Market Analysis",icon:"📈",prompt:"Analyze consumer behavior and market trends for: "},
  ];
  const run=async()=>{
    if(!query.trim())return; setLoading(true); setResult("");
    try{
      const m=modes.find(x=>x.id===mode);
      const res=await claude([{role:"user",content:`${m.prompt}"${query}"\n\nProvide: 1) Current analysis with real data, 2) Key indicators and metrics, 3) 30-90 day forecast, 4) Risk level, 5) Recommended actions for policymakers.`}],true,
        "You are a Global Public Mind AI analyst. Provide structured, data-driven analysis with real-world insights.");
      setResult(res);
    }catch(e){setResult("Analysis failed. Please retry.");}
    setLoading(false);
  };
  return(
    <div>
      <SH label="AI PREDICTION & ANALYTICS ENGINE"/>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(130px,1fr))",gap:"8px",marginBottom:"16px"}}>
        {modes.map(m=>(
          <div key={m.id} onClick={()=>setMode(m.id)} style={{
            background:mode===m.id?"rgba(218,119,255,0.15)":"rgba(255,255,255,0.03)",
            border:`1px solid ${mode===m.id?"rgba(218,119,255,0.5)":"rgba(255,255,255,0.08)"}`,
            borderRadius:"10px",padding:"12px",cursor:"pointer",textAlign:"center",transition:"all 0.2s"}}>
            <div style={{fontSize:"20px",marginBottom:"5px"}}>{m.icon}</div>
            <div style={{fontSize:"10px",color:mode===m.id?"#DA77FF":"rgba(200,220,255,0.5)",fontWeight:"700"}}>{m.label}</div>
          </div>
        ))}
      </div>
      <div style={{display:"flex",gap:"8px",marginBottom:"14px"}}>
        <Input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==="Enter"&&run()} placeholder={`Enter topic for ${modes.find(m=>m.id===mode)?.label}…`}/>
        <Btn onClick={run} color="#DA77FF" disabled={loading}>{loading?"◉…":"ANALYZE"}</Btn>
      </div>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(240px,1fr))",gap:"10px",marginBottom:"14px"}}>
        {AI_CAPABILITIES.map((cap,i)=>(
          <div key={i} style={{background:"rgba(255,255,255,0.02)",border:`1px solid ${cap.color}25`,borderRadius:"12px",padding:"14px 16px"}}>
            <div style={{display:"flex",alignItems:"center",gap:"8px",marginBottom:"8px"}}>
              <span style={{fontSize:"20px"}}>{cap.icon}</span>
              <span style={{fontSize:"11.5px",fontWeight:"800",color:cap.color}}>{cap.title}</span>
            </div>
            <p style={{fontSize:"11px",color:"rgba(200,220,255,0.6)",lineHeight:"1.65",margin:0}}>{cap.desc}</p>
          </div>
        ))}
      </div>
      {loading&&<div style={{textAlign:"center",padding:"28px",color:"#DA77FF",fontSize:"11px",letterSpacing:"2px"}}>◉ AI ANALYZING + WEB SEARCH ACTIVE…</div>}
      {result&&<Card color="#DA77FF"><div style={{fontSize:"10px",color:"#DA77FF",letterSpacing:"2px",marginBottom:"10px"}}>🤖 AI ANALYSIS RESULT</div><div style={{fontSize:"12px",color:"rgba(220,235,255,0.85)",lineHeight:"1.8",whiteSpace:"pre-wrap"}}>{result}</div></Card>}
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: AI CHAT  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function ChatTab(){
  const [msgs,setMsgs]=useState([{role:"assistant",content:"👋 Hello! I'm Claude, AI for GRT Assist Digital Campaign. I'm connected to the Digital Campus, Student Environment, Lab Systems and Public Mind Platform. How can I help you today?"}]);
  const [input,setInput]=useState("");
  const [loading,setLoading]=useState(false);
  const [ws,setWs]=useState(true);
  const endRef=useRef(null);
  useEffect(()=>{endRef.current?.scrollIntoView({behavior:"smooth"});},[msgs]);
  const send=async()=>{
    if(!input.trim()||loading)return;
    const m={role:"user",content:input};
    const nm=[...msgs,m]; setMsgs(nm); setInput(""); setLoading(true);
    try{
      const r=await claude(nm.map(x=>({role:x.role,content:x.content})),ws,"You are Claude AI for GRT Assist Digital Campaign — Digital Campus, Student Environment and Public Mind Platform. Be helpful and concise.");
      setMsgs(prev=>[...prev,{role:"assistant",content:r}]);
    }catch(e){setMsgs(prev=>[...prev,{role:"assistant",content:"Connection error. Retry."}]);}
    setLoading(false);
  };
  const qs=["Explain Smart Classroom","Lab safety checklist","Public mind analytics","Virtual lab features","Student performance factors","Research innovation hub"];
  return(
    <div style={{display:"flex",flexDirection:"column",height:"520px"}}>
      <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:"8px"}}>
        <div style={{fontSize:"10px",color:"rgba(200,220,255,0.35)",letterSpacing:"2px"}}>GRT AI CHAT — CLAUDE API LIVE</div>
        <label style={{display:"flex",alignItems:"center",gap:"6px",cursor:"pointer",fontSize:"10px",color:"#43E97B"}}>
          <input type="checkbox" checked={ws} onChange={e=>setWs(e.target.checked)} style={{accentColor:"#43E97B"}}/>
          WEB SEARCH {ws?"ON":"OFF"}
        </label>
      </div>
      <div style={{display:"flex",gap:"6px",flexWrap:"wrap",marginBottom:"8px"}}>
        {qs.map((q,i)=><span key={i} onClick={()=>setInput(q)} style={{fontSize:"10px",background:"rgba(0,150,255,0.08)",border:"1px solid rgba(0,150,255,0.2)",borderRadius:"20px",padding:"4px 10px",cursor:"pointer",color:"rgba(0,201,255,0.7)"}}>{q}</span>)}
      </div>
      <div style={{flex:1,overflowY:"auto",display:"flex",flexDirection:"column",gap:"10px",marginBottom:"10px",paddingRight:"4px"}}>
        {msgs.map((m,i)=>(
          <div key={i} style={{display:"flex",justifyContent:m.role==="user"?"flex-end":"flex-start"}}>
            <div style={{maxWidth:"80%",background:m.role==="user"?"rgba(0,150,255,0.2)":"rgba(255,255,255,0.05)",border:`1px solid ${m.role==="user"?"rgba(0,150,255,0.4)":"rgba(255,255,255,0.1)"}`,borderRadius:m.role==="user"?"14px 14px 4px 14px":"14px 14px 14px 4px",padding:"12px 14px"}}>
              {m.role==="assistant"&&<div style={{fontSize:"10px",color:"#DA77FF",marginBottom:"5px",letterSpacing:"1px"}}>🤖 CLAUDE × GRT</div>}
              <div style={{fontSize:"12.5px",color:"rgba(220,235,255,0.88)",lineHeight:"1.7",whiteSpace:"pre-wrap"}}>{m.content}</div>
            </div>
          </div>
        ))}
        {loading&&<div style={{display:"flex"}}><div style={{background:"rgba(255,255,255,0.04)",border:"1px solid rgba(255,255,255,0.1)",borderRadius:"14px 14px 14px 4px",padding:"12px 16px"}}><div style={{fontSize:"10px",color:"#DA77FF",marginBottom:"6px"}}>🤖 CLAUDE × GRT</div><div style={{display:"flex",gap:"4px"}}>{[0,1,2].map(j=><div key={j} style={{width:"6px",height:"6px",borderRadius:"50%",background:"#00C9FF",animation:`bounce 0.8s ${j*0.2}s infinite`}}/>)}</div></div></div>}
        <div ref={endRef}/>
      </div>
      <div style={{display:"flex",gap:"8px"}}>
        <Input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&!e.shiftKey&&send()} placeholder="Ask about campus, labs, public mind, student environment…"/>
        <Btn onClick={send} color="#DA77FF" disabled={loading}>SEND</Btn>
      </div>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: ROOT SEARCH  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
const SEARCH_IDX=[
  ...CAMPUS_LAYERS.flatMap(l=>[{type:"Campus",label:l.label,icon:l.icon,color:l.color},...l.nodes.map(n=>({type:"Node",label:n.label,icon:l.icon,color:l.color,desc:n.desc}))]),
  ...STUDY_ENV.map(e=>({type:"Study Env",label:e.label,icon:e.icon,color:e.color,desc:`${e.types.length} environment types`})),
  ...LAB_ENV.map(l=>({type:"Lab",label:l.label,icon:l.icon,color:l.color,desc:`${l.subs.length} sub-labs`})),
  ...SCHOOL_ENV.map(e=>({type:"School",label:e.label,icon:e.icon,color:e.color})),
  ...UNI_LABS.map(l=>({type:"Uni Lab",label:l.label,icon:l.icon,color:l.color})),
  ...MIND_LAYERS.map(l=>({type:"Mind Layer",label:l.label,icon:l.icon,color:l.color,desc:l.definition.slice(0,80)})),
  ...AI_CAPABILITIES.map(c=>({type:"AI Cap",label:c.title,icon:c.icon,color:c.color,desc:c.desc})),
  {type:"Company",label:"GRT Assist Digital Campaign",icon:"🛡️",color:"#00C9FF",desc:"Automation and Security Pvt Ltd"},
];
function SearchTab(){
  const [q,setQ]=useState("");
  const [answer,setAnswer]=useState("");
  const [loading,setLoading]=useState(false);
  const filtered=q.trim()?SEARCH_IDX.filter(i=>i.label.toLowerCase().includes(q.toLowerCase())||i.type.toLowerCase().includes(q.toLowerCase())||(i.desc||"").toLowerCase().includes(q.toLowerCase())):[];
  const search=async()=>{
    if(!q.trim())return; setLoading(true); setAnswer("");
    try{ const r=await claude([{role:"user",content:`Root search: "${q}" — in context of Digital Campus, Student Labs, School Environment and Public Mind. Give key facts and current data.`}],true); setAnswer(r);}
    catch(e){setAnswer("Search error.");}
    setLoading(false);
  };
  return(
    <div>
      <SH label="ROOT SEARCH — LOCAL + AI + WEB"/>
      <div style={{display:"flex",gap:"8px",marginBottom:"14px"}}>
        <Input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&search()} placeholder={`Search across ${SEARCH_IDX.length} indexed items…`}/>
        <Btn onClick={search} color="#00C9FF" disabled={loading}>{loading?"…":"SEARCH"}</Btn>
      </div>
      {!q&&<div style={{display:"flex",flexWrap:"wrap",gap:"6px",marginBottom:"14px"}}>{SEARCH_IDX.slice(0,12).map((item,i)=><span key={i} onClick={()=>setQ(item.label)} style={{fontSize:"10px",background:"rgba(255,255,255,0.04)",border:"1px solid rgba(255,255,255,0.1)",borderRadius:"20px",padding:"5px 12px",cursor:"pointer",color:"rgba(200,220,255,0.55)"}}>{item.icon} {item.label}</span>)}</div>}
      {filtered.length>0&&<div style={{display:"flex",flexDirection:"column",gap:"8px",marginBottom:"14px"}}>{filtered.map((item,i)=>(
        <div key={i} style={{display:"flex",alignItems:"center",gap:"12px",background:"rgba(255,255,255,0.03)",border:`1px solid ${item.color}25`,borderRadius:"10px",padding:"12px 14px"}}>
          <span style={{fontSize:"18px"}}>{item.icon}</span>
          <div style={{flex:1}}>
            <div style={{display:"flex",gap:"8px",alignItems:"center",marginBottom:"3px"}}>
              <span style={{fontSize:"12px",fontWeight:"800"}}>{item.label}</span>
              <Tag label={item.type} color={item.color}/>
            </div>
            {item.desc&&<div style={{fontSize:"11px",color:"rgba(200,220,255,0.5)"}}>{item.desc.slice(0,90)}{item.desc.length>90?"…":""}</div>}
          </div>
        </div>
      ))}</div>}
      {loading&&<div style={{textAlign:"center",padding:"24px",color:"#00C9FF",fontSize:"11px",letterSpacing:"2px"}}>◉ AI + WEB SEARCH ACTIVE…</div>}
      {answer&&<Card color="#00C9FF"><div style={{fontSize:"10px",color:"#00C9FF",letterSpacing:"2px",marginBottom:"10px"}}>🤖 AI + WEB ANSWER</div><div style={{fontSize:"12px",color:"rgba(220,235,255,0.85)",lineHeight:"1.8",whiteSpace:"pre-wrap"}}>{answer}</div></Card>}
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: LIVE FEED  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function LiveTab(){
  const [feed,setFeed]=useState([]);
  const [loading,setLoading]=useState(false);
  const [topic,setTopic]=useState("EdTech AI digital campus 2025");
  const topics=["EdTech AI 2025","Digital campus labs","Public mind analytics","Smart classroom tech","India university innovation","Lab safety compliance","Student mental health","School technology 2025"];
  const fetch_=async(t)=>{
    setLoading(true);setFeed([]);
    try{
      const raw=await claude([{role:"user",content:`Search web for 5 latest news about: "${t}". Return ONLY JSON array: [{title,source,summary,date,category}]. No markdown.`}],true,"Return only valid JSON array.");
      let parsed=[];
      try{parsed=JSON.parse(raw.replace(/```json|```/g,"").trim());}catch{const m=raw.match(/\[[\s\S]*\]/);if(m)parsed=JSON.parse(m[0]);}
      setFeed(Array.isArray(parsed)?parsed:[]);
    }catch(e){setFeed([]);}
    setLoading(false);
  };
  useEffect(()=>{fetch_(topic);},[]);
  return(
    <div>
      <SH label="LIVE INTELLIGENCE FEED"/>
      <div style={{display:"flex",flexWrap:"wrap",gap:"6px",marginBottom:"12px"}}>
        {topics.map((t,i)=><span key={i} onClick={()=>{setTopic(t);fetch_(t);}} style={{fontSize:"10px",background:topic===t?"rgba(0,150,255,0.15)":"rgba(255,255,255,0.04)",border:`1px solid ${topic===t?"rgba(0,150,255,0.5)":"rgba(255,255,255,0.1)"}`,color:topic===t?"#00C9FF":"rgba(200,220,255,0.5)",borderRadius:"20px",padding:"5px 12px",cursor:"pointer",transition:"all 0.2s"}}>{t}</span>)}
      </div>
      <div style={{display:"flex",justifyContent:"flex-end",marginBottom:"12px"}}>
        <Btn onClick={()=>fetch_(topic)} color="#FF77B3" style={{padding:"7px 14px",fontSize:"10px"}}>↺ REFRESH</Btn>
      </div>
      {loading&&<div style={{textAlign:"center",padding:"30px",color:"#00C9FF",fontSize:"11px",letterSpacing:"2px"}}>◉ FETCHING LIVE INTELLIGENCE…</div>}
      <div style={{display:"flex",flexDirection:"column",gap:"10px"}}>
        {feed.map((item,i)=>(
          <div key={i} style={{background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.08)",borderRadius:"12px",padding:"16px"}}>
            <div style={{display:"flex",justifyContent:"space-between",gap:"10px",marginBottom:"8px"}}>
              <div style={{fontSize:"13px",fontWeight:"700",lineHeight:"1.4",flex:1}}>{item.title}</div>
              <Tag label={item.category||"NEWS"} color="#00C9FF"/>
            </div>
            <p style={{fontSize:"11.5px",color:"rgba(200,220,255,0.65)",lineHeight:"1.6",margin:"0 0 8px"}}>{item.summary}</p>
            <div style={{display:"flex",gap:"12px",fontSize:"10px",color:"rgba(200,220,255,0.35)"}}><span>📰 {item.source}</span><span>🕐 {item.date}</span></div>
          </div>
        ))}
        {!loading&&feed.length===0&&<div style={{textAlign:"center",padding:"30px",color:"rgba(200,220,255,0.25)",fontSize:"11px",letterSpacing:"2px"}}>SELECT TOPIC OR CLICK REFRESH</div>}
      </div>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: SUPPORT  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
const SUGGS=["How to access virtual lab?","Reset campus portal password","Lab safety equipment list","Smart classroom setup help","Online exam not loading","Internship portal access","WiFi connectivity issue","Mental health support resources","Research data access request","Grade appeal process"];
function SupportTab(){
  const [input,setInput]=useState("");
  const [suggs,setSuggs]=useState([]);
  const [cat,setCat]=useState("General");
  const [prio,setPrio]=useState("Normal");
  const [ticket,setTicket]=useState(null);
  const [aiRes,setAiRes]=useState("");
  const [loading,setLoading]=useState(false);
  const [history,setHistory]=useState([]);
  const cats=["General","Technical","Academic","Financial","Mental Health","Lab Safety","Security","Career","Research","Public Mind"];
  const prios=["Low","Normal","High","Critical"];
  const pc={Low:"#43E97B",Normal:"#00C9FF",High:"#F7971E",Critical:"#FF6B6B"};
  useEffect(()=>{setSuggs(input.trim()?SUGGS.filter(s=>s.toLowerCase().includes(input.toLowerCase())).slice(0,4):[]);},[input]);
  const submit=async()=>{
    if(!input.trim())return; setLoading(true);
    const t={id:`TKT-${Date.now().toString().slice(-5)}`,issue:input,cat,prio,time:new Date().toLocaleTimeString(),status:"Processing"};
    setTicket(t);setHistory(h=>[t,...h.slice(0,4)]);setInput("");setSuggs([]);
    try{
      const r=await claude([{role:"user",content:`Support: GRT Assist Digital Campaign\nCategory: ${cat}\nPriority: ${prio}\nIssue: "${input}"\nProvide: 1) Immediate resolution steps, 2) Contact/escalation, 3) Expected time, 4) Self-service resources.`}],false,"Support AI for GRT Assist Digital Campaign. Be empathetic and solution-focused.");
      setAiRes(r);setHistory(h=>h.map(x=>x.id===t.id?{...x,status:"Resolved"}:x));
    }catch(e){setAiRes("Ticket logged. Team responds within 24 hours.");}
    setLoading(false);
  };
  return(
    <div>
      <SH label="SMART SUPPORT — AUTOCOMPLETE + AI RESOLUTION"/>
      <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:"8px",marginBottom:"12px"}}>
        <div>
          <div style={{fontSize:"10px",color:"rgba(200,220,255,0.4)",letterSpacing:"1px",marginBottom:"5px"}}>CATEGORY</div>
          <select value={cat} onChange={e=>setCat(e.target.value)} style={{width:"100%",background:"rgba(255,255,255,0.05)",border:"1px solid rgba(255,255,255,0.15)",borderRadius:"8px",padding:"9px 10px",color:"#E0F0FF",fontSize:"11px",fontFamily:"inherit"}}>{cats.map(c=><option key={c} value={c} style={{background:"#0A1525"}}>{c}</option>)}</select>
        </div>
        <div>
          <div style={{fontSize:"10px",color:"rgba(200,220,255,0.4)",letterSpacing:"1px",marginBottom:"5px"}}>PRIORITY</div>
          <select value={prio} onChange={e=>setPrio(e.target.value)} style={{width:"100%",background:"rgba(255,255,255,0.05)",border:`1px solid ${pc[prio]}50`,borderRadius:"8px",padding:"9px 10px",color:pc[prio],fontSize:"11px",fontFamily:"inherit"}}>{prios.map(p=><option key={p} value={p} style={{background:"#0A1525"}}>{p}</option>)}</select>
        </div>
      </div>
      <div style={{position:"relative",marginBottom:"12px"}}>
        <textarea value={input} onChange={e=>setInput(e.target.value)} placeholder="Describe issue… (autocomplete active)" rows={3}
          style={{width:"100%",background:"rgba(255,255,255,0.05)",border:"1px solid rgba(0,150,255,0.3)",borderRadius:"10px",padding:"12px 14px",color:"#E0F0FF",fontSize:"12px",outline:"none",resize:"none",fontFamily:"inherit",boxSizing:"border-box"}}/>
        {suggs.length>0&&<div style={{position:"absolute",top:"100%",left:0,right:0,zIndex:10,background:"#0A1525",border:"1px solid rgba(0,150,255,0.3)",borderRadius:"0 0 10px 10px",overflow:"hidden"}}>
          {suggs.map((s,i)=><div key={i} onClick={()=>{setInput(s);setSuggs([]);}} style={{padding:"9px 14px",fontSize:"11.5px",color:"rgba(200,220,255,0.75)",cursor:"pointer",borderBottom:"1px solid rgba(255,255,255,0.05)"}}>💡 {s}</div>)}
        </div>}
      </div>
      <Btn onClick={submit} disabled={loading||!input.trim()} color="#00C9FF" style={{width:"100%",padding:"13px",letterSpacing:"2px",opacity:!input.trim()?0.4:1,marginBottom:"14px"}}>
        {loading?"◉ AI PROCESSING…":"SUBMIT SUPPORT TICKET"}
      </Btn>
      {aiRes&&ticket&&<Card color="#00C9FF" style={{marginBottom:"14px"}}><div style={{display:"flex",justifyContent:"space-between",marginBottom:"10px"}}><div style={{fontSize:"10px",color:"#00C9FF",letterSpacing:"2px"}}>🎫 {ticket.id} — AI RESOLUTION</div><Tag label="RESOLVED" color="#43E97B"/></div><div style={{fontSize:"12px",color:"rgba(220,235,255,0.8)",lineHeight:"1.75",whiteSpace:"pre-wrap"}}>{aiRes}</div></Card>}
      {history.length>0&&<div><div style={{fontSize:"10px",color:"rgba(200,220,255,0.35)",letterSpacing:"2px",marginBottom:"8px"}}>TICKET HISTORY</div>{history.map(t=><div key={t.id} style={{display:"flex",justifyContent:"space-between",alignItems:"center",background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.07)",borderRadius:"8px",padding:"10px 14px",marginBottom:"6px"}}><div><div style={{fontSize:"11px",fontWeight:"700",marginBottom:"2px"}}>{t.id} — {t.cat}</div><div style={{fontSize:"10px",color:"rgba(200,220,255,0.45)"}}>{t.issue.slice(0,50)}{t.issue.length>50?"…":""}</div></div><div style={{textAlign:"right"}}><div style={{fontSize:"9px",color:pc[t.prio]}}>{t.prio}</div><div style={{fontSize:"9px",color:"#43E97B",marginTop:"2px"}}>{t.status}</div></div></div>)}</div>}
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: API KEYS  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function ApiTab(){
  const [copied,setCopied]=useState("");
  const [saved,setSaved]=useState({});
  const [inp,setInp]=useState({});
  const copy=(text,label)=>{navigator.clipboard.writeText(text);setCopied(label);setTimeout(()=>setCopied(""),2000);};
  return(
    <div>
      <SH label="API KEYS — SETUP GUIDE & WEB CONNECTIVITY"/>
      <div style={{background:"rgba(255,193,7,0.07)",border:"1px solid rgba(255,193,7,0.25)",borderRadius:"12px",padding:"12px 16px",marginBottom:"16px",fontSize:"11.5px",color:"rgba(255,215,0,0.75)",lineHeight:"1.7"}}>
        ⚠️ <strong>Security:</strong> Never share API keys publicly. Store in .env files and restrict keys to your domain.
      </div>
      <div style={{display:"flex",flexDirection:"column",gap:"14px"}}>
        {API_KEYS_GUIDE.map((api,i)=>(
          <div key={i} style={{background:`rgba(${rgb(api.color)},0.05)`,border:`1px solid ${api.color}25`,borderRadius:"14px",padding:"18px 20px"}}>
            <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:"12px",flexWrap:"wrap",gap:"8px"}}>
              <div style={{display:"flex",alignItems:"center",gap:"10px"}}>
                <span style={{fontSize:"24px"}}>{api.icon}</span>
                <div>
                  <div style={{fontSize:"14px",fontWeight:"800",color:api.color}}>{api.service}</div>
                  <div style={{fontSize:"10px",color:"rgba(200,220,255,0.45)",marginTop:"2px"}}>{api.purpose}</div>
                </div>
              </div>
              <div style={{display:"flex",gap:"6px",flexWrap:"wrap",alignItems:"center"}}>
                <Tag label={api.free} color={api.color}/>
                <a href={api.link} target="_blank" rel="noreferrer" style={{fontSize:"10px",background:`rgba(${rgb(api.color)},0.12)`,color:api.color,border:`1px solid ${api.color}40`,borderRadius:"4px",padding:"3px 10px",textDecoration:"none"}}>OPEN CONSOLE ↗</a>
              </div>
            </div>
            <div style={{marginBottom:"12px"}}>
              <div style={{fontSize:"10px",color:api.color,letterSpacing:"2px",marginBottom:"8px"}}>SETUP STEPS</div>
              {api.steps.map((step,j)=>(
                <div key={j} style={{display:"flex",gap:"10px",alignItems:"flex-start",marginBottom:"5px"}}>
                  <div style={{width:"18px",height:"18px",borderRadius:"50%",background:`rgba(${rgb(api.color)},0.2)`,color:api.color,fontSize:"9px",fontWeight:"800",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0}}>{j+1}</div>
                  <span style={{fontSize:"11.5px",color:"rgba(200,220,255,0.7)",lineHeight:"1.5"}}>{step}</span>
                </div>
              ))}
            </div>
            <div style={{display:"flex",gap:"8px",alignItems:"center",flexWrap:"wrap"}}>
              <span style={{fontSize:"10px",color:"rgba(200,220,255,0.35)"}}>FORMAT: <span style={{color:api.color}}>{api.keyFormat}</span></span>
              <input value={inp[i]||""} onChange={e=>setInp(p=>({...p,[i]:e.target.value}))} placeholder={`Paste ${api.service} key…`}
                style={{flex:1,minWidth:"160px",background:"rgba(0,0,0,0.3)",border:`1px solid ${api.color}30`,borderRadius:"6px",padding:"7px 10px",color:"#E0F0FF",fontSize:"11px",outline:"none",fontFamily:"inherit"}}/>
              <button onClick={()=>{setSaved(s=>({...s,[i]:inp[i]}));copy(inp[i]||"",api.service);}} style={{background:`rgba(${rgb(api.color)},0.15)`,border:`1px solid ${api.color}40`,color:api.color,borderRadius:"6px",padding:"7px 14px",cursor:"pointer",fontSize:"10px",letterSpacing:"1px",fontFamily:"inherit"}}>
                {copied===api.service?"✓ SAVED":"SAVE KEY"}
              </button>
            </div>
            {saved[i]&&<div style={{fontSize:"10px",color:"#43E97B",marginTop:"6px"}}>✓ {api.service} key saved</div>}
          </div>
        ))}
      </div>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: GITHUB + APK  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function GitHubTab(){
  const [copied,setCopied]=useState("");
  const copy=(text,label)=>{navigator.clipboard.writeText(text);setCopied(label);setTimeout(()=>setCopied(""),2000);};
  const cmds=[
    {label:"Create React PWA",code:"npx create-react-app grt-assist-digital-campaign --template cra-template-pwa\ncd grt-assist-digital-campaign\nnpm install recharts lucide-react"},
    {label:".env Setup",code:"REACT_APP_ANTHROPIC_KEY=sk-ant-api03-YOUR_KEY\nREACT_APP_GOOGLE_KEY=AIzaSy-YOUR_KEY\nREACT_APP_NEWS_KEY=YOUR_NEWSAPI_KEY\nREACT_APP_WEATHER_KEY=YOUR_WEATHER_KEY"},
    {label:"Push to GitHub",code:"git init\ngit add .\ngit commit -m 'GRT Assist Digital Campaign v4'\ngit branch -M main\ngit remote add origin https://github.com/YOUR_USERNAME/grt-assist-digital-campaign.git\ngit push -u origin main"},
    {label:"Deploy to Vercel",code:"npm i -g vercel\nnpm run build\nvercel --prod\n# Add .env vars in Vercel Dashboard → Settings → Env Variables"},
    {label:"PWA to APK via PWABuilder",code:"1. Deploy your app to Vercel first\n2. Open https://www.pwabuilder.com\n3. Enter: https://YOUR-APP.vercel.app\n4. Click 'Build My PWA' → Android\n5. Download .apk package\n6. Enable Developer Mode on Android → Install APK"},
  ];
  return(
    <div>
      <SH label="GITHUB + WEB DEPLOY + APK GENERATION"/>
      <div style={{display:"flex",flexDirection:"column",gap:"10px",marginBottom:"20px"}}>
        {GITHUB_STEPS.map(s=>(
          <div key={s.step} style={{display:"flex",gap:"14px",background:"rgba(255,255,255,0.02)",border:"1px solid rgba(255,255,255,0.07)",borderRadius:"12px",padding:"14px 16px"}}>
            <div style={{width:"32px",height:"32px",borderRadius:"50%",background:"linear-gradient(135deg,#0055CC,#7B2FFF)",display:"flex",alignItems:"center",justifyContent:"center",fontSize:"16px",flexShrink:0}}>{s.icon}</div>
            <div style={{flex:1}}>
              <div style={{fontSize:"12px",fontWeight:"800",color:"#00C9FF",marginBottom:"4px"}}>STEP {s.step}: {s.title}</div>
              <div style={{fontSize:"11.5px",color:"rgba(200,220,255,0.65)",lineHeight:"1.6",whiteSpace:"pre-line"}}>{s.desc}</div>
            </div>
          </div>
        ))}
      </div>
      <div style={{display:"flex",flexDirection:"column",gap:"10px",marginBottom:"16px"}}>
        {cmds.map((c,i)=>(
          <div key={i} style={{background:"rgba(0,0,0,0.4)",border:"1px solid rgba(255,255,255,0.1)",borderRadius:"12px",overflow:"hidden"}}>
            <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"10px 14px",background:"rgba(255,255,255,0.04)",borderBottom:"1px solid rgba(255,255,255,0.08)"}}>
              <span style={{fontSize:"11px",color:"#43E97B",letterSpacing:"1px"}}>{c.label}</span>
              <button onClick={()=>copy(c.code,c.label)} style={{background:"rgba(67,233,123,0.1)",border:"1px solid rgba(67,233,123,0.3)",color:"#43E97B",borderRadius:"4px",padding:"4px 10px",cursor:"pointer",fontSize:"10px",letterSpacing:"1px",fontFamily:"inherit"}}>{copied===c.label?"✓ COPIED":"COPY"}</button>
            </div>
            <pre style={{margin:0,padding:"14px",fontSize:"11px",color:"rgba(200,220,255,0.75)",overflowX:"auto",lineHeight:"1.7",fontFamily:"'Courier New',monospace"}}>{c.code}</pre>
          </div>
        ))}
      </div>
      <Card color="#F7971E">
        <div style={{fontSize:"10px",color:"#F7971E",letterSpacing:"2px",marginBottom:"12px"}}>⚠️ IMPORTANT NOTES</div>
        {["APK binary files require Android Studio OR use PWABuilder.com for web-to-APK conversion",
          "Add GitHub username to generate exact git remote URL — replace YOUR_USERNAME in commands above",
          "Never commit .env files to GitHub — add .env to your .gitignore before first push",
          "Vercel free tier: 100GB bandwidth/month, custom domains supported",
          "For Google Play Store publish: Google Play Console (play.google.com/console) — $25 one-time registration",
          "Share your GitHub username with GRT Assist Digital Campaign for team collaboration access"].map((note,i)=>(
          <div key={i} style={{display:"flex",gap:"8px",alignItems:"flex-start",marginBottom:"7px"}}>
            <span style={{color:"#F7971E",flexShrink:0}}>▸</span>
            <span style={{fontSize:"11.5px",color:"rgba(200,220,255,0.7)",lineHeight:"1.5"}}>{note}</span>
          </div>
        ))}
      </Card>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  TAB: ABOUT US  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
function AboutTab(){
  const [copied,setCopied]=useState("");
  const copy=(text,label)=>{navigator.clipboard.writeText(text);setCopied(label);setTimeout(()=>setCopied(""),2000);};
  const services=[
    {icon:"🤖",title:"AI Integration",desc:"Embedding Claude and other AI into campus and enterprise for automated decision-making."},
    {icon:"🔐",title:"Security Solutions",desc:"End-to-end cybersecurity, identity management, data encryption and compliance."},
    {icon:"⚙️",title:"Process Automation",desc:"Smart workflow engines, RPA solutions and auto-completion systems."},
    {icon:"🏛",title:"Digital Campus Platform",desc:"Full-stack LMS, ERP, virtual labs and AI-powered student lifecycle management."},
    {icon:"🌍",title:"Public Mind Analytics",desc:"Multi-layer environment monitoring, sentiment analysis and behavior prediction."},
    {icon:"🧪",title:"Lab Environment Systems",desc:"Micro lab setup, safety compliance, smart lab management for schools and universities."},
  ];
  return(
    <div>
      <div style={{background:"linear-gradient(135deg,rgba(0,150,255,0.12),rgba(218,119,255,0.08))",border:"1px solid rgba(0,150,255,0.35)",borderRadius:"20px",padding:"28px",marginBottom:"18px",position:"relative",overflow:"hidden"}}>
        <div style={{position:"absolute",top:"-60px",right:"-60px",width:"200px",height:"200px",background:"radial-gradient(circle,rgba(0,150,255,0.12) 0%,transparent 70%)",pointerEvents:"none"}}/>
        <div style={{display:"flex",alignItems:"flex-start",gap:"18px",flexWrap:"wrap",position:"relative"}}>
          <div style={{width:"76px",height:"76px",borderRadius:"16px",background:"linear-gradient(135deg,#0055CC,#7B2FFF)",display:"flex",alignItems:"center",justifyContent:"center",fontSize:"34px",boxShadow:"0 0 28px rgba(0,100,255,0.35)",flexShrink:0}}>🛡️</div>
          <div style={{flex:1}}>
            <div style={{fontSize:"10px",color:"#00C9FF",letterSpacing:"3px",marginBottom:"6px"}}>── REGISTERED PRIVATE LIMITED COMPANY ──</div>
            <h2 style={{margin:"0 0 4px",fontSize:"clamp(16px,3vw,26px)",fontWeight:"900",background:"linear-gradient(135deg,#FFFFFF,#00C9FF)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent",lineHeight:1.2}}>{COMPANY.name}</h2>
            <h3 style={{margin:"0 0 10px",fontSize:"clamp(10px,2vw,13px)",fontWeight:"700",color:"rgba(200,220,255,0.65)",letterSpacing:"1px"}}>AUTOMATION AND SECURITY PVT LTD</h3>
            <p style={{margin:0,fontSize:"12px",color:"rgba(200,220,255,0.55)",lineHeight:"1.7"}}>GRT Assist Digital Campaign drives intelligent automation, AI-powered digital transformation and robust security solutions — empowering campuses, enterprises and institutions across India through cutting-edge digital campaigns and smart technology.</p>
          </div>
        </div>
      </div>
      <div style={{display:"grid",gridTemplateColumns:"repeat(4,1fr)",gap:"10px",marginBottom:"16px"}}>
        {[{num:"369+",label:"Projects"},{num:"100%",label:"Compliance"},{num:"24/7",label:"AI Support"},{num:"13+",label:"Platforms"}].map((s,i)=>(
          <div key={i} style={{background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.08)",borderRadius:"12px",padding:"14px",textAlign:"center"}}>
            <div style={{fontSize:"clamp(16px,3vw,22px)",fontWeight:"900",background:"linear-gradient(135deg,#00C9FF,#DA77FF)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent",marginBottom:"4px"}}>{s.num}</div>
            <div style={{fontSize:"10px",color:"rgba(200,220,255,0.4)",letterSpacing:"1px"}}>{s.label}</div>
          </div>
        ))}
      </div>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(200px,1fr))",gap:"10px",marginBottom:"16px"}}>
        {[
          {icon:"📧",label:"Email",value:COMPANY.email,link:`mailto:${COMPANY.email}`,color:"#00C9FF",cv:COMPANY.email},
          {icon:"📞",label:"Phone",value:COMPANY.phoneDisplay,link:`tel:${COMPANY.phone}`,color:"#43E97B",cv:COMPANY.phone},
          {icon:"🌐",label:"Website",value:"www.grt.com",link:"https://www.grt.com",color:"#DA77FF",cv:"www.grt.com"},
        ].map((c,i)=>(
          <div key={i} style={{background:`rgba(${rgb(c.color)},0.06)`,border:`1px solid ${c.color}30`,borderRadius:"14px",padding:"16px 18px"}}>
            <div style={{display:"flex",alignItems:"center",gap:"8px",marginBottom:"8px"}}><span style={{fontSize:"20px"}}>{c.icon}</span><span style={{fontSize:"11px",color:c.color,fontWeight:"700",letterSpacing:"1px"}}>{c.label}</span></div>
            <div style={{fontSize:"14px",fontWeight:"800",marginBottom:"10px"}}>{c.value}</div>
            <div style={{display:"flex",gap:"6px"}}>
              <a href={c.link} target="_blank" rel="noreferrer" style={{flex:1,textAlign:"center",background:`rgba(${rgb(c.color)},0.12)`,border:`1px solid ${c.color}40`,color:c.color,borderRadius:"8px",padding:"7px",fontSize:"10px",textDecoration:"none",display:"block"}}>OPEN ↗</a>
              <button onClick={()=>copy(c.cv,c.label)} style={{flex:1,background:"rgba(255,255,255,0.05)",border:"1px solid rgba(255,255,255,0.1)",color:copied===c.label?"#43E97B":"rgba(200,220,255,0.5)",borderRadius:"8px",padding:"7px",fontSize:"10px",cursor:"pointer",fontFamily:"inherit"}}>{copied===c.label?"✓ COPIED":"COPY"}</button>
            </div>
          </div>
        ))}
      </div>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(240px,1fr))",gap:"10px",marginBottom:"16px"}}>
        {services.map((s,i)=>(
          <div key={i} style={{background:"rgba(255,255,255,0.02)",border:"1px solid rgba(255,255,255,0.07)",borderRadius:"12px",padding:"14px 16px",transition:"all 0.25s"}}
            onMouseEnter={e=>{e.currentTarget.style.background="rgba(0,150,255,0.07)";e.currentTarget.style.borderColor="rgba(0,150,255,0.3)";}}
            onMouseLeave={e=>{e.currentTarget.style.background="rgba(255,255,255,0.02)";e.currentTarget.style.borderColor="rgba(255,255,255,0.07)";}}>
            <div style={{display:"flex",alignItems:"center",gap:"8px",marginBottom:"8px"}}><span style={{fontSize:"20px"}}>{s.icon}</span><span style={{fontSize:"11.5px",fontWeight:"800",color:"#00C9FF"}}>{s.title}</span></div>
            <p style={{fontSize:"11px",color:"rgba(200,220,255,0.6)",lineHeight:"1.65",margin:0}}>{s.desc}</p>
          </div>
        ))}
      </div>
      <div style={{textAlign:"center",padding:"20px",background:"rgba(255,255,255,0.02)",border:"1px solid rgba(255,255,255,0.06)",borderRadius:"14px"}}>
        <div style={{fontSize:"10px",color:"rgba(200,220,255,0.3)",letterSpacing:"3px",marginBottom:"8px"}}>OUR MISSION</div>
        <p style={{fontSize:"13px",fontWeight:"700",lineHeight:"1.7",margin:"0 0 12px",background:"linear-gradient(135deg,#00C9FF,#DA77FF)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent"}}>
          "Empowering institutions through intelligent automation,<br/>uncompromising security and AI-first digital transformation."
        </p>
        <div style={{fontSize:"10px",color:"rgba(200,220,255,0.2)",letterSpacing:"2px"}}>{COMPANY.full} · DIGITAL CAMPAIGN · INDIA</div>
      </div>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════════════════════════
// ░░░  MAIN APP  ░░░
// ═══════════════════════════════════════════════════════════════════════════════
const ALL_TABS=[
  ["campus","🏛","Campus"],["study","📚","Study Env"],["lab","🔬","Labs"],
  ["microlab","🧪","Micro Lab"],["school","🏫","School"],["unilab","🎓","Uni Labs"],
  ["mind","🌍","Public Mind"],["ai","🤖","AI Predict"],["chat","💬","Chat"],
  ["search","🔍","Search"],["live","📡","Live Feed"],["support","🎫","Support"],
  ["api","🔑","API Keys"],["github","💻","GitHub+APK"],["about","🏢","About Us"],
];

export default function App(){
  const [tab,setTab]=useState("campus");
  const [time,setTime]=useState(new Date());
  useEffect(()=>{const t=setInterval(()=>setTime(new Date()),1000);return()=>clearInterval(t);},[]);
  return(
    <div style={{fontFamily:"'Courier New',monospace",background:"#050A14",minHeight:"100vh",color:"#E0F0FF",padding:"16px 20px"}}>
      <style>{`*{box-sizing:border-box;}::-webkit-scrollbar{width:4px;}::-webkit-scrollbar-thumb{background:rgba(0,150,255,0.3);border-radius:4px;}select option{background:#0A1525;}@keyframes bounce{0%,80%,100%{transform:scale(0)}40%{transform:scale(1)}}`}</style>
      <div style={{position:"fixed",inset:0,backgroundImage:"linear-gradient(rgba(0,150,255,0.03) 1px,transparent 1px),linear-gradient(90deg,rgba(0,150,255,0.03) 1px,transparent 1px)",backgroundSize:"40px 40px",pointerEvents:"none",zIndex:0}}/>
      <div style={{position:"fixed",top:"-150px",left:"50%",transform:"translateX(-50%)",width:"900px",height:"500px",background:"radial-gradient(ellipse,rgba(0,100,255,0.08) 0%,transparent 70%)",pointerEvents:"none",zIndex:0}}/>
      <div style={{position:"relative",zIndex:1,maxWidth:"1200px",margin:"0 auto"}}>
        {/* Header */}
        <div style={{textAlign:"center",marginBottom:"14px"}}>
          <div style={{fontSize:"9px",color:"rgba(200,220,255,0.3)",letterSpacing:"3px",marginBottom:"4px"}}>{COMPANY.full}</div>
          <h1 style={{fontSize:"clamp(18px,4vw,42px)",fontWeight:"900",letterSpacing:"-1px",lineHeight:1.1,margin:"0 0 4px",background:"linear-gradient(135deg,#FFFFFF 0%,#00C9FF 45%,#DA77FF 100%)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent"}}>
            GRT ASSIST DIGITAL CAMPAIGN
          </h1>
          <p style={{color:"rgba(200,220,255,0.35)",fontSize:"10px",margin:0,letterSpacing:"2px"}}>
            DIGITAL CAMPUS · PUBLIC MIND · LABS · v4.0 · {SEARCH_IDX.length}+ INDEXED ITEMS · CLAUDE AI · WEB SEARCH · PWA/APK READY
          </p>
        </div>
        {/* Status */}
        <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",background:"rgba(0,150,255,0.05)",border:"1px solid rgba(0,150,255,0.15)",borderRadius:"8px",padding:"6px 14px",fontSize:"10px",color:"rgba(200,220,255,0.4)",letterSpacing:"1.5px",marginBottom:"12px",flexWrap:"wrap",gap:"4px"}}>
          <span>🛡️ GRT ASSIST DIGITAL CAMPAIGN · CLAUDE SONNET 4 · v4.0</span>
          <span style={{color:"#43E97B"}}>● {ALL_TABS.length} TABS ACTIVE</span>
          <span>{time.toLocaleTimeString()} · {new Date().toLocaleDateString()}</span>
        </div>
        {/* Tabs */}
        <div style={{display:"flex",gap:"5px",flexWrap:"wrap",marginBottom:"16px",justifyContent:"center"}}>
          {ALL_TABS.map(([id,icon,label])=>(
            <button key={id} onClick={()=>setTab(id)} style={{
              background:tab===id?"rgba(0,150,255,0.2)":"rgba(255,255,255,0.03)",
              border:`1px solid ${tab===id?"rgba(0,150,255,0.6)":"rgba(255,255,255,0.1)"}`,
              color:tab===id?"#00C9FF":"rgba(200,220,255,0.4)",
              borderRadius:"8px",padding:"7px 11px",fontSize:"10px",letterSpacing:"0.8px",cursor:"pointer",transition:"all 0.2s",
              display:"flex",alignItems:"center",gap:"4px",fontFamily:"inherit"}}>
              {icon} {label}
            </button>
          ))}
        </div>
        {/* Content */}
        <div style={{minHeight:"500px"}}>
          {tab==="campus"   && <CampusTab/>}
          {tab==="study"    && <StudyTab/>}
          {tab==="lab"      && <LabTab/>}
          {tab==="microlab" && <MicroLabTab/>}
          {tab==="school"   && <SchoolTab/>}
          {tab==="unilab"   && <UniLabTab/>}
          {tab==="mind"     && <MindTab/>}
          {tab==="ai"       && <AIPredTab/>}
          {tab==="chat"     && <ChatTab/>}
          {tab==="search"   && <SearchTab/>}
          {tab==="live"     && <LiveTab/>}
          {tab==="support"  && <SupportTab/>}
          {tab==="api"      && <ApiTab/>}
          {tab==="github"   && <GitHubTab/>}
          {tab==="about"    && <AboutTab/>}
        </div>
        {/* Footer */}
        <div style={{marginTop:"22px",borderTop:"1px solid rgba(255,255,255,0.06)",paddingTop:"12px",display:"flex",justifyContent:"center",gap:"12px",flexWrap:"wrap",fontSize:"10px",color:"rgba(200,220,255,0.2)",letterSpacing:"1.5px"}}>
          <span>🛡️ {COMPANY.name}</span><span>·</span>
          <span style={{color:"#43E97B"}}>● ONLINE</span><span>·</span>
          <span>{COMPANY.email}</span><span>·</span>
          <span>{COMPANY.phoneDisplay}</span><span>·</span>
          <span>v4.0 — {SEARCH_IDX.length} ITEMS INDEXED</span>
        </div>
      </div>
    </div>
  );
}
